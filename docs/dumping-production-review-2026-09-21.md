# 클린광진 상황실(`/dumping`) 구청장·국장 시연 전 프로덕션 최종 점검 (2026-09-21, 19라운드)

대상: 2026-09-22 구청장·국장 보고(덱 `chris-gomdori/260922`, 8장 시연 안내 4단계) 직전의 프로덕션 `gjdong.vercel.app/dumping`. 점검 시점 프로덕션 배포 = main HEAD 7551059(Vercel 배포 `dpl_2cFsWFZYzX119MihNfLhR6yp3w7R`, icn1, 05:14 KST). 반영 결과는 6절, 시연 당일 준비는 5절.

## 0. 증거와 범위

| 증거 | 내용 |
|---|---|
| 배포 정합 | Vercel 프로덕션 커밋 SHA = 로컬 main HEAD. 데이터 라우트 4종(`map`·`interventions`·`bin-recos` 깊은 비교 동일, `graph` 정오표 적용분만 다름·노드 105·연결 169 동일) |
| 정적 게이트 | `tsc` 0 · `eslint components/dumping lib/dumping app/dumping app/api/dumping` 0 · `tests/dumping-*.test.ts` 57/57. (전체 테스트는 다른 세션의 `/snow` 미커밋분과 섞여 여기서 안 돌림) |
| 실제 렌더 | Playwright로 프로덕션 로그인 뒤 덱 8장 4단계 + 앱 시연 모드 5장면 + 탭 5종 + 모달 2종 + 지도 토글 3종을 1920×1080(헤드리스)·1512×982(헤드 Chrome, 실제 GPU) 캡처. `docs/dumping-ux-audit/r19-*` |
| 라이브 API | 프로덕션 쿠키로 `ask`(Gemini 스트리밍)·`tts` 직접 호출 |
| 음성 | 헤드 Chrome + 가짜 마이크: `Permissions-Policy` 통과·`getUserMedia` ok·`SpeechRecognition.start()` started-ok |
| 성능 | 헤드 Chrome(Apple M5, ANGLE Metal) RAF fps를 장면별로 실측, CDP 프로파일로 병목 위치 확인, 로컬 dev(3001)에서 레이어·유리 요소를 하나씩 끄며 이분 탐색 |

하지 않은 것: 실기기 스피커·마이크 음성 시연, 휴대폰 폭(시연 무관), 냉독 2인(14라운드 이후 카피 변화 없음), 비공개 저장소 데이터 재수출.

## 1. 종합 판정

| 항목 | 판정 | 한 줄 사유 |
|---|---|---|
| 시연 4단계(덱 8장) | **가능** | 정책 제안 › 수거 시간대 조정 › 지도(청소차 노선), 물어보기 시드 즉답, 운영·전망 핫스팟 20 기둥·CCTV 없음 14곳, 근거 그래프 다가구·단독 밀집 선택(13 남고 92 흐림) 전부 프로덕션에서 동작(2절) |
| 앱 시연 모드 5장면 | **가능** | 결론 › 동별 비교 › 상습격자 › 예측 드론 › 정책 제안. 캡션 수치 map.json과 일치. 헤드리스 캡처의 장면 5 핫스팟 기둥 잔존은 스위프트셰이더 지연 산물, 헤드 Chrome에서 깨끗함 |
| 음성·라이브 답 | **가능** | 마이크 정책·인식 시작 ok. 준비된 답 0초, 모델 답 첫 글자 1.0초·완료 17초, TTS 2.6초(3초 분량) |
| 화면 간 숫자 | **1건 불일치** | 데이터·방법 창이 자료 **23종**(구청 7·공개 10·서울시 6). 덱은 22종(공개 9). 16라운드에 "지도 바탕·건물 입체·지형"이 공개 목록에 추가됨(3절 결정 2) |
| 성능 | **결정 필요** | 회전·드론 비행 중 17~27fps. 병목은 지도가 아니라 유리 굴절(`backdrop-filter: url()`). 움직일 때 굴절만 쉬게 하면 46~56fps(3절 결정 1) |
| 보안·공개 노출 | 변화 없음 | 14라운드 반영 상태 유지. 비로그인 데이터 라우트 401, CSP·Permissions-Policy 헤더 그대로 |

## 2. 시연 4단계·5장면 실측 (P0 없음)

| 덱 8장 단계 | 실측(프로덕션, 1512·1920) | 비고 |
|---|---|---|
| 1 정책 제안 결론 두 줄·제안 6건 › 수거 시간대 조정 › 제안 이유·지도 | 결론 2문장, 제안 6(추가 예산 없음 3·저비용 2·예산 필요 1). 모달 "제안 이유·가정한 작동 원리·기대 방향·예산·담당·효과 확인 방법". 버튼 **"지도에서 청소차 노선과 함께 보기"** › 과태료 바탕 + 앰버 노선 + 청소차, 왼쪽에 "반영 중" 띠 | 반영 해제는 오른쪽 열 ✕ |
| 2 물어보기 "김주임" › "CCTV는 어디에 놓아야 하나" | 핵심 질문 6개 중 하나. 클릭 즉시 "늘려서 줄인다는 근거는 확인되지 않았습니다 … 재배치 후보 20곳" + 근거 수치·한계 + 듣기 버튼 | 호출어 대기는 페이지를 열 때마다 귀 버튼으로 켜야 함(저장 안 됨). 마이크 권한은 Chrome이 처음 한 번 물음 |
| 3 운영·전망 핫스팟 20 기둥 · CCTV 없음 | 탭을 열면 20곳 기둥+순위 라벨이 바로 섬. 목록 20행 중 CCTV 없음 배지 14, 집중관리 배지 포함. 1위 클릭 › 카메라 이동 + 라벨 "예측 핫스팟 1위 · 광진구 동일로72길 18-6" | 적중률 63.1% · 포착률 9% · 무작위 2.8% |
| 4 근거 그래프 "다가구·단독 밀집" | 같은 이름의 노드가 **둘**(요인·개념 / 분석 변수). 검색창에 "다가구" › 목록에서 **요인·개념** 선택 = 연결 12(증거·대책·KPI·주체) 남고 나머지 흐림. 분석 변수 쪽은 증거 9개만 | 구면 그래프는 천천히 돌아 직접 찍기 어려움. 검색 경로 권장 |
| 앱 시연 모드 5장면 | ←→·Esc 동작, 캡션: 결론 / 화양동 민원 469·과태료 525 1위 / 상습격자 32·앱 제외 11 / 적중률 63.1%·상위 5곳 드론 / CCTV 재배치 후보 20곳 | 시연 버튼은 폭 1280 이상에서만 |
| 드론 비행·동별 막대·격자 기둥 토글 | 켜짐·꺼짐·안내 띠 정상, 콘솔 오류 0 | |
| 조치 대장 | "등록된 조치가 없습니다" (interventions.json의 예시 항목은 화면에 안 나옴) | 덱 9장 "조치 대장 등록"과 어긋나지 않음 |

콘솔: `Error: dem dimension mismatch`(maplibre `backfillBorder`) 5회. 지형 타일 경계에서 나는 내부 오류로 화면 영향 없음(캡처 전부 정상). `pmtiles` `ERR_ABORTED`는 이동 중 취소된 범위 요청.

## 3. 결정 필요 (추천안)

| # | 항목 | 추천 | 대안 |
|---|---|---|---|
| 1 | **회전·비행 중 fps** 17~27 | **(A) 지도가 움직이는 동안 굴절 필터만 쉬고 흐림 유리로**: `dumping-map`이 `move` 이벤트로 `html.lg-moving`을 붙였다 240ms 뒤 떼고, CSS가 그동안 `.lg-shell`의 `url()` 필터를 `.dump-fl`과 같은 흐림으로 덮음. 로컬 실측 25~32 › 56fps. 멈추면 굴절 복귀. 코드 20줄 안팎, 오늘 배포 | (B) 코드 없이 시연 전 유리 다이얼을 0으로(불투명 종이 패널, 64fps). (C) 그대로(20fps대, 드론 비행이 끊겨 보임) |
| 2 | **자료 수 22 vs 23** | **(A) 앱: "지도 바탕·건물 입체·지형"을 공개 수집 종수에서 빼고 각주로**(분석 수치에 안 쓰는 자료라고 이미 적혀 있음) › 22종 유지, 덱 무수정 | (B) 덱을 23으로: 표지 HUD·6장 칩 22개·카운트업 22·수치표 갱신(여러 곳) |
| 3 | 근거 그래프 동명 노드 | 코드 무수정, 5절 순서에 "요인·개념" 선택을 적음 | 정오표(`errata.ts`)로 분석 변수 쪽 라벨에 "(회귀 변수)"를 붙임 |

## 4. 실측 수치

| 항목 | 값 | 조건 |
|---|---|---|
| 첫 로드 | 로그인 폼 0.55초, 로그인 › 탭 0.23~0.36초, 지도 캔버스 1.1~1.6초, 타일·건물·아이콘 안정 ~10초, 전송 10.4MB | 헤드 Chrome, 이 맥(M5) |
| fps 정지 | 120(화면 주사율) | |
| fps 회전(장면 1·2·3) | 22~27 | DPR 1·2 같음 = 픽셀 수가 아니라 프레임마다 다시 계산하는 굴절 필터가 병목 |
| fps 드론(장면 4) | 16~17 | |
| fps 장면 5·청소차 | 17~23 | |
| 이분 탐색(로컬 dev, 회전) | 지형 끔 31 · 건물 끔 29 · 전 레이어 끔 31 · **굴절 끔 46~56** · 유리 0(lg-flat) 64 | 병목 = `.lg-shell` 10개의 `backdrop-filter: url(#feDisplacementMap)` |
| 모델 답(`ask`) | 첫 글자 1.0초, 완료 17.6초 | "화양동은 왜 많나?" |
| TTS | 2.6초 / 144KB(L16 24kHz 3초) | |
| JS 힙 | 시작 146MB › 시연 뒤 244MB | 누수 징후 없음(두 번째 실행 176MB) |

## 5. 시연 당일 준비

1. **브라우저는 Chrome**(Safari는 굴절 유리 폴백·음성 인식 제한). 시연 전에 `gjdong.vercel.app/dumping`을 열어 로그인하고 탭 5개를 한 번씩 눌러 둔다(타일·3D 모델 캐시). 새로고침하면 로딩 커튼(4단계)이 다시 뜨고 걷힌 뒤 결론이 3초 동안 단계로 등장한다(실사 건물 › 다가구·단독 초록 › 과태료 기둥). 그 3초는 손대지 않는다.
2. **비밀번호는 Vercel 프로덕션 값**. 로컬 `.env.local`의 4자리와 **다르다**(실측). 로그인 한도 IP당 분당 10회.
3. 물어보기: 귀 버튼(호출어 대기)을 켜고 Chrome 마이크 허용을 한 번 눌러 둔다. 회장 소음이 크면 "김주임" 인식이 흔들리므로 실패 시 목록의 "CCTV는 어디에 놓아야 하나?"를 누른다(덱 8장 문구 그대로). 즉석 질문은 모델 답이라 완료까지 15~20초, 첫 문장은 1초.
4. 근거 그래프: 검색창 "다가구" › **다가구·단독 밀집 · 요인·개념** 선택. 그래프에서 직접 찍지 않는다.
5. 운영·전망: 탭만 열어도 기둥 20개가 선다. 1위 행을 누르면 카메라가 내려간다.
6. 네트워크: 전부 Vercel(icn1). 회장 Wi-Fi 불안 시 휴대폰 핫스팟. 음성 인식도 인터넷 필요.
7. 화면: 폭 1280 이상이어야 "시연" 버튼이 보인다. 프로젝터 미러링 시 1920×1080 기준으로 확인함. 시연 중 **H 키(또는 캡션 바 "카드 숨김")**로 왼쪽 카드를 숨기면 지도가 넓어지고 카메라가 다시 가운데로 온다. 다시 H면 복귀.
8. 유리 강도 다이얼(물방울 아이콘)은 localStorage에 저장된다. 굴절 쉼이 배포됐으니 기본값(1) 그대로 둔다. 더 매끄럽게 가려면 0(불투명 종이).

## 6. 반영 (결정 1·2 모두 추천안, 같은 날)

| 결정 | 변경 | 검증 |
|---|---|---|
| 1 굴절 쉼 | [dumping-map.tsx](../components/dumping/dumping-map.tsx) 초기화 효과에 `map.on("move")` → `html.lg-moving` 추가, 마지막 move 240ms 뒤 제거, 언마운트 시 정리. [globals.css](../app/globals.css) `.lg-on.lg-moving:not(.lg-flat) .dump-page .lg-shell`을 `.dump-fl`과 같은 흐림으로 `!important` 덮기, `.lg-inner` 흐림 없음 | 로컬 dev(3001) 헤드 Chrome: 정지 `lg-on` › 회전 `lg-on lg-moving`(쉘 computed `blur(24px) saturate(1.5) brightness(1.04)`, 인라인 url 그대로 남아 멈추면 복귀) › 멈춤 1.2초 뒤 `lg-on`. 회전 25~33 › 50~52fps, 시연 5장면 31~35fps(같은 환경 반영 전 16~27) |
| 2 자료 22종 | [methods-modal.tsx](../components/dumping/methods-modal.tsx) `collected`에서 "지도 바탕·건물 입체·지형" 제거, `DatasetGroup`에 `note` 추가해 공개 수집 아래 각주(OSM Protomaps · Mapzen 지형 · 국토교통부 GIS건물통합정보, 분석 수치에 안 씀) | 창 본문 "구청 7종 · 공개 데이터 직접 수집 9종 · 서울시 6종" + 각주 확인 |
| 게이트 | `tsc` 0 · eslint(dumping 4경로) 0 · 테스트 57/57 | 로컬 `next build`는 다른 세션이 3000에 띄운 `next start`의 `.next`를 덮으므로 생략, Vercel 빌드로 대체 |

### 6-1. 프로덕션 재확인 (cb170c9, Vercel `dpl_846E5yFmEbKRcDUo21VHVAf22Jnd` READY, `gjdong.vercel.app`)

헤드 Chrome 1920×1080, 같은 맥: 정지 `lg-on`(쉘 `url(#lg-5) …`, 안쪽 `blur(10px)`) › 회전 `lg-on lg-moving`(쉘 `blur(24px) saturate(1.5) brightness(1.04)`, 안쪽 none) › 멈춤 1.2초 뒤 원복 › 시연 5장면 뒤에도 원복. fps 회전 38, 장면 1~5 = 32·33·31·29·30(반영 전 27·26·23·16·19). 데이터·방법 창 "구청 7종 · 공개 9종 · 서울시 6종" + 각주. 콘솔 오류 0(`dem dimension mismatch` 제외). 5절 준비 항목은 그대로.

### 6-2. 같은 날 추가(사용자 요청): 로딩 커튼 · 첫 화면 단계 등장 · 시연 중 카드 숨김

| 요청 | 변경 | 검증(로컬 dev, 헤드 Chrome) |
|---|---|---|
| 초기 화면 로딩 프로그레스 | 지도 영역만 종이 커튼(`.dump-curtain`, z 1030, 왼쪽 카드·탭은 그대로). 4단계를 실제 이벤트로 체크: 01 자료(fetch 완료) · 02 지도 바탕(`load`) · 03 건물 24,520동 결합(첫 `idle`) · 04 시설·청소차 3D(`iconsReady`). `DumpingMap onStage` 콜백 신설. 04 또는 12초 상한에서 0.65초 페이드. 자료 오류 시 즉시 걷힘 | 0.5초 01 진행 › 1.1초 02 › 2.5초 04 › 2.8초 페이드 › 3.7초 제거. 탭 왕복 뒤 재등장 없음 |
| 첫 화면 = 결론의 단계 등장 | 초기 view는 실사 건물(`base none`·원 없음). 커튼이 걷히면 0.8초 뒤 다가구·단독 초록(paint 전환 300ms), 1.8초 뒤 과태료 기둥 솟음(기존 `riseColumns`). 사용자가 그 사이 바탕·원을 바꿨으면 건드리지 않음(기능적 setView 가드). 끝 상태 = 종전 기본 화면 = 시연 장면 1 | 프레임 0·0.6·1.1·2.0·2.4·3.2초 캡처로 순서 확인(`reveal-*.png`) |
| 시연 중 왼쪽 카드 숨김 | 캡션 바 "카드 숨김/보기" 버튼 + `H`(ㅎ) 키. 숨기면 `aside`·폭 손잡이 display none, 캡션 바·월별 띠가 16px부터, `fitPadding.left` 24. `DumpingMap padSeq`로 `easeTo({ padding })` 0.7초 재중심(드론 비행 중엔 즉시 `setPadding`, 프레임마다 jumpTo가 easeTo를 끊어서). 시연을 나가면 자동 복귀 + padding 원복 | H › aside none·캡션 left 16px·padding 480→24 › H › 복귀. 버튼도 동일. 장면 4(드론)에서도 동작. 줌은 그대로라 다음 장면부터 새 폭에 맞춰 잡힘 |

프로덕션 재확인(f6188f6, `dpl_J2zHJHbtienDtFzfLLns8ivwCLs2`, 헤드 Chrome): 커튼 01 0.3초 › 02 0.6초 › 04 2.1초 › 페이드 2.4초 › 제거 3.1초, 범례 다가구·단독으로 전환, 탭 왕복 뒤 재등장 없음. 카드 숨김 H·버튼·장면 4·Esc 복귀 전부 동작, 콘솔 오류 0.

### 6-3. /snow 5라운드에서 가져온 것(사용자 "참고해봐")

| 항목 | 변경 | 검증(로컬 dev, 헤드 Chrome) |
|---|---|---|
| 3D 핀 깜박임 | [icons3d.ts](../components/dumping/icons3d.ts) `render()`에서 `gl.clear(DEPTH_BUFFER_BIT)` 뒤 그림(/snow 0ed2bdd와 같음). 핀·시설·청소차가 건물 뒤로 들락거리지 않고 마커처럼 늘 위 | 장면 5 회전 중 앰버 핀·숫자·보라 카메라 전부 온전(`tiles3-scene5-pins`) |
| 타일 진행률 | [dumping-map.tsx](../components/dumping/dumping-map.tsx) 첫 idle까지 `dataloading`/`data`/`error`의 벡터·DEM 타일만 집계해 `onTiles`. 커튼 02·03 단계에 "지도 타일 n/m", 막대는 단계 25% + 타일 비율, 오른쪽에 % | 3Mbps 스로틀: 02 타일 0→56(25→47%) › `load` 12.9초 › 03 타일 →92(50→73%) › 17.5초 걷힘. 상한 12초 → **25초**(3Mbps에서 `load`가 12초를 넘겼다) |

가져오지 않은 것: 사파리 DEM 512·음영 지연(지형이 같은 DEM을 씀, 시연은 Chrome), data-early(로그인 뒤 fetch 0.3초), 2D 번호 배지(입체 숫자는 사용자 선택), 지도 한가운데 유리 카드(커튼이 있어야 결론 등장이 된다).

## 7. 남은 일

- 14·16라운드 이월: 조치 대장 실제 등록(청소과), 프로덕션 비밀번호 4자리 유지 결정, 배치추천 시각화.
- `dem dimension mismatch`: 지형 타일 경계 원인 조사(DEM pmtiles z8~14 256px, 화면 영향 없음).
