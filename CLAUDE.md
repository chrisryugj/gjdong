# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Korean address converter tool using Kakao Local API. Converts addresses to standardized formats (road name, jibun, administrative dong) with map visualization. Four main surfaces:
1. **Main Converter** (`/`) - Single/batch address conversion with Excel export
2. **Facility Dashboard** (`/facility`) - Map-based facility manager: paste/upload any facility table (column order auto-detected), filter by admin-dong/category, search, colored markers, Excel/screenshot/report export. Data persists in `localStorage` only.
3. **Tableau Geocoder** (`/tableau-geocoder`) - CSV/Excel file upload for bulk geocoding (adds lat/lon columns)
4. **Chrome Extension** (`extension/`) - Browser extension for instant address conversion (popup, right-click, shortcut, auto-detect)
5. **Crowd Radar / 인파레이더** (`/crowd`) - Real-time congestion dashboard for **5 cities, 239 spots** (Seoul 121 · Jeju 66 · Busan 26 · Gangwon east coast 18 · Incheon Airport gates 8). Architecture after the 2026-08 refactor:
   - **Adapters** ([lib/crowd/](lib/crowd/)): one file per city (`seoul-rtd.ts`, `jeju.ts`, `busan.ts`, `gangwon.ts`, `incheon.ts`), each with a "데이터 계약 실측" header comment (the de-facto tech doc — never delete). Shared helpers in [lib/crowd/adapter-kit.ts](lib/crowd/adapter-kit.ts) (toNum, parkRatioLv 0.6/0.8/0.95, meanRoadLv, fetchMeteo12h, fetchBeachInfo, createSnapshot, emptyDetailFields). Server routing goes through `ADAPTERS` registry ([lib/crowd/adapters.ts](lib/crowd/adapters.ts), server-only); client UI branches only on `CITY_CAPS` capability table ([lib/crowd/cities.ts](lib/crowd/cities.ts)). Adding a city = adapter file + one entry in each. Grade basis differs per city (`CrowdSpot.basis`: ppl/access/wait/none) and is disclosed in list/map/cards — methodology in [docs/crowd-methodology.md](docs/crowd-methodology.md), adoption guide in [docs/crowd-adoption-guide.md](docs/crowd-adoption-guide.md).
   - **Dashboard** ([components/crowd/crowd-dashboard.tsx](components/crowd/crowd-dashboard.tsx), ~530 lines) composes hooks in [components/crowd/hooks/](components/crowd/hooks/): use-crowd-data (city + polling, `CITY_CAPS.pollMinutes`, hidden-tab pause with alerts-armed exception except Jeju), use-spot-selection (history/deep-link policy), use-spot-filters, use-persisted-prefs, use-split-pane, use-install-prompt, use-watchlist, use-ops-mode, use-crowd-alerts, use-time-lens (map-only day×hour pattern recolor from the cumulative heatmap; loader shared with spot-heatmap via [lib/crowd/heatmap-client.ts](lib/crowd/heatmap-client.ts), heatmap cities only, list/header stay live). Map view auto-fits the city's real spot bbox once per city on data arrival (`fitCity` prop, dashboard only — ops minimap/report keep `fitView`); wheel zoom uses `zoomSnap` 0.25 + `wheelPxPerZoomLevel` (Leaflet ignores `zoomDelta` for wheel — keep it default 1 so +/- buttons stay full-step).
   - **Ops mode** (상황실, [components/crowd/ops/](components/crowd/ops/), dynamic import): watchlist up to 12 spots (localStorage per city, `?mode=ops&spots=` deep link = session override), card grid busy-first with trend arrows, district bulk-add ([lib/crowd/districts.ts](lib/crowd/districts.ts) static mapping — regenerate via `scripts/generate-crowd-districts.ts` with Kakao key), CSV/XLSX/situation-report export ([lib/crowd/export.ts](lib/crowd/export.ts), Korean-fixed output; XLSX = snapshot+event-log sheets via click-time `import("xlsx")` — SheetJS CE writes no freeze/fill), busy-transition browser alerts ([lib/crowd/alerts.ts](lib/crowd/alerts.ts) seed/disarm/re-arm/cooldown), event timeline log ([lib/crowd/oplog.ts](lib/crowd/oplog.ts) + use-ops-log — polling snapshots to per-city localStorage, 288-tick cap, zero extra API calls).
   - **Print report** (`/crowd/report?city=&spots=`, [components/crowd/report/](components/crowd/report/)): A4 공문서-style situation report, Korean-fixed, `buildReportModel` in export.ts; data etiquette mirrors ops (detail fan-out Seoul-only watch spots, extra only when spots given), sparkline column from the ops event log when ≥2 ticks. MBTI picks ([lib/crowd/mbti.ts](lib/crowd/mbti.ts), citizen mode Seoul-only) is an entertainment feature — deterministic category/level scoring, always-visible disclaimer, never in work outputs.
   - **Incheon airport board** ([components/crowd/airport-board.tsx](components/crowd/airport-board.tsx), dynamic import, list-bottom `extra` slot of SpotListPanel): parking zones + arrivals (pickup) + taxi queue + bus/limousine timetables via `/api/crowd/airport` ([lib/crowd/incheon-airport.ts](lib/crowd/incheon-airport.ts), keyless airport.kr internals — arrivals `getArrPasSchList.do` POST with hex layout constant, taxi = SSR table on `/ap_ko/987/subview.do` (T1 row is `<th>`, T2 is `<td>`, fare-table decoys exist — anchor on "대기 차량(대)"), bus `busRouteList.do`/`busInfoDetail.do` (detail HTML carries no reliable route name — display the clicked route's name from the list), arrival-exit passenger forecast `statisticPredictCrowdedOfInout.do` (daily 17:00 forecast, T1 groups A,B/C/D/E,F · T2 A/B), AREX timetable parsed from airportrailroad.com `/train/normal/info/{100|110}/0` SSR (span `data-id` A010=express·B010=all-stop-to-Seoul only; attributes are newline-indented so the tag-close regex must allow ~400 chars; Sat/Sun→holiday table, public holidays not detected)).
   - **data.go.kr layer** (2026-08-09, one account key `DATA_GO_KR_KEY`, each service needs 활용신청; unapproved = silent empty): [lib/crowd/safety.ts](lib/crowd/safety.ts) 기상특보(getPwnStatus — ⚠️stnId does NOT filter, every office returns the same nationwide bulletin; parse 시도 segments with paren-aware splitting, "제외" parens = in effect, drop 먼바다 segments, gangwon needs inner coastal-town match) + 재난문자 merged into the existing `disaster` banner/report for ALL cities (seoul keeps RTD messages + warnings only). 재난문자 primary = **keyless safetydata.go.kr 알림 SSR page** (`/disaster-data/disasterNotification?keyword=시도명` — keyword searches a hidden recipient-region field, verified live; today-only filter, incheon excludes non-영종 sender 구s exactly), fallback = V2 API DSSP-IF-00247 (needs data.go.kr 15134001 approval, auto-activates). [lib/crowd/air.ts](lib/crowd/air.ts) 에어코리아 시도별 실시간(1 call/sido, 15-min snapshot + stale-on-error; ⚠️gateway kills reused keep-alive connections after big responses — use krgovFetch not undici fetch, and numOfRows=60 not 200 which 504s; station mapping = district→station candidates verified live, station name always disclosed). [lib/crowd/events.ts](lib/crowd/events.ts) TourAPI searchFestival2(⚠️old areaCode param silently returns 0 — use `lDongRegnCd` 법정동 codes 11/26/28/50/51; ⚠️response carries raw control chars, strip [\x00-\x1F] before JSON.parse; 120-day lookback then filter ongoing/≤14d-upcoming). Spot↔event matching is client-side ([lib/crowd/events-client.ts](lib/crowd/events-client.ts)) so the events route never calls adapter fetchSpots (jeju origin protection). Seoul subway arrivals: keyless RTD `subway?hotspotNm=` endpoint folded into `fetchSpotExtra` (`parseSubwayRows`). "지금 vs 평소" baseline badges: `baselineDelta` in heatmap-client + use-baseline hook (heatmap cities, ≥3 samples only). CITY_CAPS gained `air`/`tourEvents`/`subway`; ops toolbar has an event-linked watch panel; report gained 특보 merge + 행사 부록.
   - Jeju source protection is a hard constraint (2026-08 blocking incident): 15-min cache/polling, no ops detail fan-out, no hidden-tab polling; datacenter IPs get empty replies so prod reads the `data-jeju` snapshot branch published by a home-network collector (`scripts/collect-jeju.ts`).
   - i18n ko/en/ja/zh: barrel [lib/crowd/i18n.ts](lib/crowd/i18n.ts) re-exports sibling files (i18n-core/-terms/-spots/-districts/-ui-{ko,en,ja,zh}/-meta). New UI strings must be added to all four `i18n-ui-*` files (UIStrings type enforces at build). API queries always use Korean spot names as keys.
   - Tests: `tests/crowd-*.test.ts` (~45 cases) pin grade-derivation boundaries, parser traps (availLots=0, "만차", "-"), registry/caps consistency, i18n symmetry, exports, alert transitions. Heatmap collection: `.github/workflows/crowd-heatmap.yml` every 3h into the orphan `data` branch.
6. **광진 라이프 / 광진구 생활상황판** (`/gwangjin`) — the crowd dashboard itself locked to pseudo-city `gwangjin` (2026-08-10 rework; the earlier standalone card page is gone). `gwangjin` is a full `CITY_IDS` member = seoul RTD subset (6 spots incl. 광나루한강공원): adapter reuses `seoulFetchDetail`/`seoulFetchDisaster` with `detail.city:"seoul"` so extra/air/tour-events ride seoul plumbing; heatmap-client aliases gwangjin→seoul data file; hidden from /crowd's switcher via `SWITCH_CITY_IDS` (deep link ?city=gwangjin still works). `CrowdDashboard fixedCity` prop (via [components/gwangjin/client.tsx](components/gwangjin/client.tsx)) pins the city, hides the switcher (`CrowdHeader lockCity`), brands the header/tab as "광진 라이프" (`gwangjinTitle`/`gwangjinSubtitle` in all four i18n-ui files), and `useCrowdData` ignores `?city=`. Gwangjin-only UI (2026-08-11 life-first rework): map life-POI layer ([components/gwangjin/use-gwangjin-life.ts](components/gwangjin/use-gwangjin-life.ts) → `CrowdMap lifePois` — 따릉이/EV/무더위쉼터/지하철역(탭=도착 팝업, lazy fetch)/응급실(고정 좌표 HOSPITAL_COORDS), default layers station+er, toggles in map control stack) + the life board as the MAIN panel replacing SpotListPanel when no search query/selection/address pin ([gwangjin-life-board.tsx](components/gwangjin/gwangjin-life-board.tsx) order: NowStrip 4-tile summary(대기·비·문연약국·응급병상, [cards-now.tsx](components/gwangjin/cards-now.tsx)) → rain/river card promoted when raining or river ratio ≥0.5 → 지하철 전광판(선택 역 localStorage `gwangjinStation` 기억) → 명소 혼잡 compact 2-col grid(click=detail, hover=map marker, baseline badges) → 응급/약국 → 역세권 상권(citydata_cmrcl 건대입구역+군자역 탭 — `CMRCL_AREAS` 실키 실측: 역세권만 지원, 공원·산 지역명은 에러, ⚠️샘플키는 지역 인자를 무시하고 동일 응답) → 공공시설 예약([lib/gwangjin/reserve.ts](lib/gwangjin/reserve.ts) — 서울 공공서비스예약 체육·문화·교육 광진 접수중분, ⚠️위치인자 필터는 실키에서도 무시라 전량 페치 후 AREANM 필터, SVCNM HTML 엔티티 디코드) → 행사 → 생활인구 → 주차; typing a search query swaps back to SpotListPanel). Hero+accordion rework (2026-08-13): the board renders only time-context heroes expanded (base subway+spots · night subway+care · weekend subway+spots+events) and every other card as a one-line summary row — `Card` in cards-live.tsx gained `summary/collapsed/onToggle` (each card computes its own summary; PopCard skips fetch while collapsed), nav chips are scroll-spy'd (DOM-position max-top rule + bottom clamp) and `expandAndJump` defers scrollIntoView by one rAF (⚠️pre-commit jump hits the collapsed-height max-scroll and strands the view). Card↔map interlink: `useGwangjinLife` owns `station`(localStorage)+`focusOnMap`(enables layer, seq-keyed MapFocus)+`onPoiTap`; CrowdMap takes `focusPoi`/`onLifePoiTap` — station chips & ER/영업중 pharmacy rows fly the map and open the marker popup, station marker taps switch the board station. ⚠️Popup race: flyTo's zoomend bumps zoomTick which clearLayers-rebuilds all life markers, destroying a just-opened popup — pendingFocus keeps a 2.5s expiry window and re-opens after each rebuild instead of clearing on first success. Map identity & layers (2026-08-11 2차): gu boundary overlay ([lib/gwangjin/boundary.ts](lib/gwangjin/boundary.ts) 537-pt static ring from southkorea/seoul-maps, `CrowdMap boundaryKey` prop dynamic-imports it — outside-dim polygon-with-hole + halo+dotted double line, pane z=330) · layer chips grouped 이동(station/bike/ev/parking)|안전·의료(er/aed/shelter)|생활(library) with separators · zoom gate `LIFE_MIN_ZOOM`(bike/ev/aed <14 hidden + bottom hint pill) · NowStrip 비 타일에 Open-Meteo 12h 최대 강수확률(`forecast` in /api/gwangjin). New adapters: `fetchLibraries`(SeoulLibraryTimeInfo 1,535행 + SeoulPublicLibraryInfo OP_TIME·HMPG_URL 조인 — ⚠️TimeInfo엔 HMPG_URL 필드 자체가 없다(실측 2026-08-13, 홈페이지 링크는 공공도서관 8곳만), 광진 44곳·[폐관] 제외 — ⚠️구 화장실류 서비스는 전부 ERROR-500 폐기) · `fetchAeds`(E-Gen 15000652, 활용신청 후 광진 416건 라이브 실측 — ⚠️미신청 서비스는 HTTP 403 → null로 구분해 KEY_GUIDES 안내) · `fetchPublicParkings`(전국주차장 표준데이터 15012896, 광진 17곳 라이브 — ⚠️서울시 GetParkingInfo엔 광진 전체가 1행뿐(구영은 자치구 관리); 실측: 봉투는 response 래핑 없는 top-level {header,body.items.item[]}, 구 필터는 insttCode=3040000만 동작(lnmadr=NODATA·insttNm=SQL 에러), 좌표 공백 7/17은 카카오 지오코딩 보충) · 약국 지도 레이어(E-Gen wgs84 좌표 실측 — 영업 중만 마커·심야 진녹색·기본 켬, 칩 숫자=영업 중 수, 팝업 길찾기 3종 공용) · 버스([lib/gwangjin/bus.ts](lib/gwangjin/bus.ts) — 정류소는 tbisMasterStation 11,492행에서 ARS "05" 프리픽스+bbox로 광진 299곳(줌 15+), 탭 도착은 ws.bus.go.kr getStationByUid(15000303 활용신청): ⚠️호스트가 **http 전용**(443 미개방)이라 krgovFetch 대신 plain fetch, ⚠️미신청은 **HTTP 200+headerCd 7+"NOT REGISTERED"(공백 표기)** — headerCd로 판정) · 경로당(전국마을회관및경로당 표준데이터 15114136, tn_ 계약 insttCode=3040000 — 서울 OA-15052는 xlsx뿐; 실측 2026-08-12: 필드는 flctNm/flctTyp/lat/lot/lctnRoadNmAddr/telno, ⚠️**광진구 미제출**로 NODATA(성북 183건으로 필터 정상 교차검증) → 빈 배열 수렴, 구가 제출하면 자동 점등) · 다크모드 타일(CARTO dark_all ↔ voyager 스왑 `darkTiles` prop — 대시보드 전용, 보고서·미니맵 불변; 구 경계선 다크에서 스카이 반전) · 도로 소통([lib/gwangjin/traffic.ts](lib/gwangjin/traffic.ts) — 2026-08-14 전체도로 개편: 주 원천 ITS trafficInfo(openapi.its.go.kr:9443, bbox 벌크·5분, 키=DATA_GO_KR_KEY, data.go.kr 15040463 활용신청 자동승인·ITS_API_KEY 오버라이드 가능, ⚠️미신청 키는 HTTP 401(4005)·apiKey=test는 bbox 무시 고정샘플 20행 — 유효성은 광진 링크셋 교집합>0으로 판정), 지오메트리는 [lib/gwangjin/road-links.json](lib/gwangjin/road-links.json)(표준노드링크 2026-08-12 전국 SHP EPSG:5186→WGS84, 광진 경계+250m 클리핑 1,561링크·66도로, 재생성 scripts/clip-gwangjin-roadlinks.mjs — zip은 its.go.kr 실세션 XHR로만 받아짐)을 클라가 동적 임포트해 [linkId,spd]와 조인·등급은 제한속도 대비 80/40%(gradeBySpeed), 폴백은 기존 RTD road?hotspotNm= 명소반경(mode:"rtd" — 보드가 KEY_GUIDES.its 안내), CrowdMap gjTraffic pane은 캔버스 렌더러(폴리라인 ~3천)). Data adapters below unchanged: Adapters in [lib/gwangjin/](lib/gwangjin/) with "데이터 계약 실측" headers (2026-08-10): `constants.ts` (RTD spots 5+1, 8 stations with official sub-named keys — "아차산" alone returns INFO-200, 15 dong codes — 자양4동 is 11215847 not ...880, code order ≠ name order), `seoul-open.ts` (openapi.seoul.go.kr:8088 envelope helper; sample key caps 5 rows/call ERROR-335 — bulk fetchers fall back to ≤5), `subway.ts` (swopenapi arrivals, must post-filter by statnNm — sample key intermittently ignores the station filter), `emergency.ts` (E-Gen ER beds + pharmacies, XML regex parse, DATA_GO_KR_KEY + 활용신청 15000563/15000576 — verified live: hv10/hv11 are "Y" flags not numbers, fields vary per hospital, 186 Gwangjin pharmacies with dutyTime{1..8}s/c), `env-safety.ts` (rain 광진구청 10-min, 중랑천 성동교 water level vs PLAN_FLDE, RealtimeCityAir, GetParkingInfo — Gwangjin has exactly 1 live-linked lot, citydata_cmrcl 건대입구역 — response shape differs: LIVE_CMRCL_STTS not row), `life.ts` (따릉이 master join via tbCycleStationInfo STA_LOC, culturalEventInfo date positional + GUNAME post-filter, 생활인구 SPOP_LOCAL_RESD_DONG — hour param must be zero-padded "00", data lags ~10 days, EV getChargerInfo zscode=11215 verified live (328 stations) + addr re-filter, 무더위쉼터 = Seoul TbGtnHwcwP OA-21065 5-page scan + AREA_CD 11215 prefix (96 shelters w/ coords; MOIS data.go.kr HeatWaveShelter3/4/5 and safemap are ALL retired as of 2026-08 — don't resurrect them)). Routes `/api/gwangjin{,/subway,/care,/daily,/pop}` split by poll cadence; missing keys degrade to null → UI renders key-application links (KEY_GUIDES). Keys: `SEOUL_OPEN_KEY` (one key covers subway/bike/rain/river/air/parking/events/pop/cmrcl), `DATA_GO_KR_KEY` shared with crowd. RTD spot 아차산 is force-mapped to 광진구 in districts.ts (generator geocodes the peak to 구리시 — keep the manual line after regeneration).
7. **클린광진 상황실** (`/dumping`, 암호 게이트 `DUMPING_PASSWORD`) — 광진구 무단투기 발생구조 분석 대시보드. 데이터·분석 SSOT는 **비공개 저장소 chrisryugj/gwangjin-dumping**(로컬 `~/workspace/gwangjin-dumping`, 원자료에 건별 민원·과태료 기록이 있어 공개 레포에 절대 커밋 금지) (README가 정본: 회귀·DID 철회·하네스·REPRODUCE 해시). 파이프라인: `build_decision_layer.py`(품목·처분퍼널·SLA·상습격자 KPI·핫스팟 백테스트·홀트윈터스 수요전망) + `fetch_permits.py`(건축HUB 인허가 구조전망, ARCHHUB_SERVICE_KEY 필요·최초본은 MCP 실측) → `export_dashboard.py`가 gjdong `data/dumping/{map,graph}.json` 생성(2026-09 이전엔 `public/dumping/` — 지금은 정적 노출 없이 인증 라우트 `/api/dumping/data/[name]`로만 서빙, 쿠키 `gj_dump_auth`=HMAC(`DUMPING_COOKIE_SECRET`, 없으면 `DUMPING_PASSWORD`), 로그인 10회/분 429). ⚠️ontology.db는 REPRODUCE 해시 대상이라 불변 — CCTV 철회·rho 재표기·의사결정 노드(DECISION_NODES) 전부 export 시점 주석 레이어. 스크립트 수정 시 `REPRODUCE/manifest.json` 해시 갱신 + `verify.py` 통과 필수. UI: [components/dumping/](components/dumping/) 탭 5개, 기본=정책 제안(policy-board — graph.json Lever/KPI 파생, 제안은 비용 등급순(무예산→저비용→예산 필요)·기존 판정·성과지표·사전등록 원칙, 상세=lever-modal)·물어보기(qa-chat: 지도앱식 검색바 + 핵심 질의응답 9 아코디언(상위 3 펼침) — 답 문장의 수치는 `buildSeeds(map, graph)`가 [lib/dumping/facts.ts](lib/dumping/facts.ts)(`summarize`·`regressionBetas`·`partialYearSuffix`·**`channelGrowth`=배율 2.10/2.97/1.11/과태료 0.53의 유일한 산출처, 마지막 해 부분연도면 연환산하고 `basis` 문장을 같이 준다**)로 파생, 청소차 제원만 README 정본 정적; 답변 최신순·지도 반영은 명시 버튼(`vizDescription`으로 예고))·발견 10카드(**`buildFindings(map, graph)` 파생** — 정적 사본이던 시절 "과태료 1.1배" 오류가 남았던 전례, 맨 위 "기존 해석 vs 이 분석" 대비 보드 contrast-panel.tsx 6쌍)·운영·전망(ops-panel: KPI/핫스팟20/전망차트/품목/퍼널/SLA/구조전망/조치대장)·전체 탐색(ontology-graph). 모달은 전부 [modal-shell.tsx](components/dumping/modal-shell.tsx) `ModalShell`(ESC·배경클릭·size md/lg/xl·zIndex·id) 위에 올린다. 운영 탭 인터랙션: 지도 가능한 건 지도로(핫스팟 20 순위배지 레이어 `showHotspots`=ops탭 상시, 상습격자 33 격자강조 `showCritical`=KPI 타일 토글, 목록 클릭=펄스 하이라이트+라벨 `CandidateFocus.label`), 나머지는 중앙 상세모달(ops-modal.tsx: funnel/channels/forecast/fines/sla/permits — ModalShell·KRW·ForecastChart 공용 export). 헤더 "데이터·방법" = methods-modal(쓰인 데이터: 구청 제공 6종/직접 수집 7종 구분 + 8개 방법론 일반인 해설). 동 브리핑 인쇄 = briefing-modal + globals.css `#dump-brief` print 규칙. 조치 대장 = `data/dumping/interventions.json`(개입 사전등록부, registeredAt 빈 항목은 예시로 UI 제외). QA 컨텍스트 = [lib/dumping/context.ts](lib/dumping/context.ts) (인과표현·what-if 금지·"인구 통제"·"실제 발생" 금지 등 해석 규칙 12개 — 수치는 facts에서 파생). **온톨로지 형식화(2026-09-05)**: [lib/dumping/schema.ts](lib/dumping/schema.ts)(클래스 14·관계 22 정의+도메인/레인지, `validateGraph` — 현재 오류 0·주의 1(cls-cell 고아)) · [lib/dumping/queries.ts](lib/dumping/queries.ts)(역량 질문 7 `runCompetencyQuestions` + `lineageOf` 근거 계보, 온톨로지 탭 onto-queries.tsx·onto-schema-modal.tsx·상세 카드 계보) · [lib/dumping/errata.ts](lib/dumping/errata.ts)(정오표 — ontology.db는 해시 잠금이라 낡은 엣지 note를 데이터 라우트·프롬프트 양쪽에서 덮어쓴다, ERR-001="과태료 1.1배") · [lib/dumping/history.ts](lib/dumping/history.ts)(QA 이력 정규화 — ⚠️cut 초기값 out.length면 후속 질문 이력 전멸). **서울시 공개데이터 층(2026-09-05)**: 비공개 레포 `scripts/build_seoul_layers.py`(OA-14991/14992 행정동 생활인구 31개월·OA-22784 250m 격자→100m 면적 배분, ★격자코드 `다사XXXXYYYY`=EPSG:5179 (900000,1900000) 원점 10m 오프셋·SHP 확증, data.go.kr 15109594 의류수거함 479·OA-2722 목적별 CCTV·OA-12051 불편신고·OA-15069 쓰레기통, ★서울 파일은 `nio_download.do` POST infId/seq/infSeq(+시트형 seqNo), 대용량 zip은 gitignore)+`regression_v2.py`(생활인구·의류수거함 추가, 200m 민감도, `.venv` statsmodels) → map.json `grid[8]`=생활인구·`dong.lp/lpf/crl/erl`·`infra.clothBins`·`decision.seoul`·`decision.regressionV2`·`kpi.criticalCellsNowNoApp`·`forecast.backtest.rows`. UI: 지도 바탕 `lp`+레이어 clothBins, 발견 카드 13(노출 통제·통념 검증·격자 검증), 대비 보드 10쌍, 운영 탭 서울 맥락 카드+`seoul` 모달, 방법 모달 "서울시" 6종 그룹+격자 해상도 FAQ, QA 시드 12. ★export는 지난주 로컬 유실분(describes 엣지 2·존댓말·ev-ledger 보정치)을 복원한 버전 — 재수출 전 `git pull`. 설계 노트·결함 8건 = [docs/dumping-ontology.md](docs/dumping-ontology.md), 출품 검토·남은 일 = [docs/dumping-contest-review.md](docs/dumping-contest-review.md). ⚠️**P0: 레포가 PUBLIC이라 `data/dumping/map.json`이 raw URL로 내려왔던 문제** — 2026-09-05 후속 세션에서 처리: **`data/dumping/map.json`은 gitignore, 커밋은 `map.json.enc`(AES-256-GCM)뿐** — `npm run build`/`dev`가 먼저 `scripts/dumping-data.mjs decrypt`(키 `DUMPING_DATA_KEY` = Vercel env production/preview + `.env.local`, 키 없고 평문 있으면 통과). ★재수출 뒤 `npm run dumping:encrypt` 후 `.enc` 커밋. 과거 이력의 map.json은 `git filter-repo`로 제거·강제 푸시(다른 클론은 재클론 필요). graph.json·interventions.json은 평문 유지. 테스트 `tests/dumping-*.test.ts`·`tests/onto-*.test.ts`가 배율·발견·정오표·이력·스키마·역량질문 답을 핀으로 박는다(그래프 재수출 시 같이 갱신). 미확보 데이터 명세 = gwangjin-dumping/docs/NEEDED_DATA.md. ⚠️`data/dumping/*.json`은 [lib/dumping/context.ts](lib/dumping/context.ts)가 빌드 시 import하므로 갱신 후 재빌드·재배포 필수. **2라운드(2026-09-05 저녁)**: ★과태료 원자료 `route`=신고 유래 83%·순찰 17% — "신고와 무관한 실측" 문구 전면 금지, 근거는 `facts.channelGrowth().finesPatrol`(순찰만 0.49배)·`finesCensorNote`(최근 월 우측 절단). map.json에 `fines.byRoute`·`kpi.thresholds`·`meta{reproduce.hashes(manifest에서), binSites}` 신설, 문장 수치는 `sampleSizes`·`collinearRange`로 파생. 온톨로지 결함 1·2·8 해결: `kpi-dump-count-cell`(격자 β)이 `kpi-dump-rate`(동 ρ·개입)를 `operationalizes`, 주장→지표 규칙은 `governs`, 기존 59노드 PROV는 export `PROV_BASE`. `queries.OUTCOMES` 집합으로 두 지표를 같이 본다(lever-view·CQ1). CQ8=PROV 공백, 검증기 `PROV_MISSING`. 지도 격자 대체 표(map-controls details). QA 고정 평가셋 `scripts/dumping-qa-eval.mjs`(프로덕션 5문항 호출, 결과 docs/dumping-qa-eval-*.md). ★`npm run build`의 decrypt가 평문을 .enc로 덮으니 재수출 뒤 반드시 `dumping:encrypt` 먼저. 발표용 통계 해설 docs/dumping-stats-explainer.md. **3라운드(2026-09-05 밤)**: ★공공데이터포털 15141768(SGIS 격자)은 1km뿐 — 100m 상주인구는 Esri KR 이미지서비스(SGIS 재배포, `build_sgis_layer.py`)를 공식 1km 40셀로 합산 대조(|차이|≤32) 후 사용. K-apt는 API 활용신청 미완이라 자료실 PNU 파일(`build_kapt_layer.py`, 세션 쿠키+CSRF+JSON POST 흐름 docstring)→건축물대장 PNU 조인. ★핵심 결과: 대장 "공동주택" 80,828세대 중 K-apt 등록 38%뿐, 세 갈래 모형(v4b)에서 연관은 다가구·단독(β +0.319)에만 있고 관리주체 없는 다세대·연립은 비유의 → 화면·프롬프트 겨냥점을 "관리주체 없는 주거"에서 "다가구·단독주택 밀집"으로 옮김(규칙 15). 상주인구는 단독·동시 모두 비유의, 다가구·단독 β 유지(규칙 14), ★200m에서만 apt_hh 유의(p=0.033) 예외 명시. `regressionV2.exposure/proxyCheck` 타입, 발견 카드 14장(대리변수 검증 신설), CQ1 STRUCTURAL_FACTORS에 con-resident-pop·con-managed-kapt, Covariate 라벨은 export COV_KO로 한글화(원변수명 노출 버그 수정). 통계 전수점검 `audit_stats.py`→docs/dumping-stats-explainer.md 15c(표본선택·Bonferroni·hurdle·노이즈·격자크기). 디자인: 정책 탭 결론 히어로(facts 파생 3수치)·섹션 번호·그림자 제거·배지 3계열·강조박스→왼쪽 액센트선·레이어 칩 xl 미만 토글·β차트 동적 높이·tabular-nums. 스크린샷은 Playwright(character-card node_modules, 로컬 `next start -p 3000` — ★3100 등 다른 포트는 proxy.ts ALLOWED_ORIGINS에 없어 로그인 실패). 출품 대회는 미확정(7.5절). **4라운드(2026-09-05 저녁)**: K-apt 단지 목록 API(15057332)+기본정보 API(15058453, 비공개 `.env` `DATA_GO_KR_KAPT_KEY`, `fetch_kapt_basis.py`) 96단지 수신 → `build_kapt_layer.py`가 자발 등록 9곳은 API 지번주소로 PNU 생성, ★공유 필지(극동1·2차)는 대장 세대 합을 API 비율로 분배, 단지별 `api`·`mandatory`·`hhDiff`·`registry`, `crossCheck.apiCompare`(정확 74/89·5% 초과 10)·`mandatory`(의무 67·자발 22)·`managedByCellApi`를 싣고 `regression_v2.py` `proxyCheck.apiSensitivity`(API 세대수 v4b, β +0.319 유지). ★export proxyCheck는 키 화이트리스트라 새 키는 792행에 추가해야 map.json에 실림. 회귀 입력은 여전히 대장 세대수. `audit_stats.py` 항목 7 공간 모형(spreg SAR ρ +0.318 β +0.269 · SEM λ +0.374 β +0.338)·8 원점 이동(200m apt_hh 유의는 (0,0) 분할뿐) → export가 audit_round3.json을 읽어 `ev-spatial-models`·`ev-kapt-api-check`·`ds-kapt-api` 노드(102노드 155엣지). ★스크립트 고치면 make_manifest 재실행(코드 해시 포함). 매니페스트 해시 113(테스트 핀). ★rtk 훅이 `npm run`을 pnpm으로 리라이트해 node_modules를 부수므로 gjdong에서 npm은 `/opt/homebrew/bin/npm` 절대경로. 검토서 8절. **5라운드 두 독자 UX(2026-09-05)**: 정책 탭 첫 화면 = 결론 한 줄 + 쉬운 꼬리표 수치 3 + 평가자 "근거 경로"(데이터 → 방법 → 결론 → 한계(해설서 GitHub) → 재현, `MethodsModal initialSection`) + 결재용 한 장 인쇄([components/dumping/policy-table.tsx](components/dumping/policy-table.tsx) `ProposalTable`·`PolicyPrintModal` `#dump-policy`, globals.css print 규칙은 `#dump-brief`와 같은 `body:has(#id)` 특이성 패턴) + 제안 표(`lever-view.proposalRows`, 레버 노드 cost·owner·verification_plan만 펼침, 없으면 "미기재"). 발견 탭은 카드 14장 먼저(`FINDING_ORDER` 결론→근거→한계), 대비 보드는 `<details>` 접힘. 탭 "온톨로지"→"근거 그래프", 헤더 그래프 규모는 탭 안으로. 390은 패널 먼저·지도 아래(`useSplitPane({mapBelow:true})`, 핸들은 패널 바닥). **변수 이름은 "다가구·단독 밀집" 하나**(정의 "건축물대장 다가구 가구+일반단독 동"은 방법 모달·지도 도움말 두 곳뿐. 옛 이름 "무관리주거"·"관리주체 없는 주거"는 export sanitizer가 db 원문까지 치환, "의무관리"는 법 용어라 보호). 시드는 [components/dumping/qa-seeds.ts](components/dumping/qa-seeds.ts)(기본 펼침 1). 카피 게이트 = [tests/dumping-copy.test.ts](tests/dumping-copy.test.ts)(옛 변수명·줄표·챗봇 말투·금지 문구 0, 카드 순서). 결측 표시는 "미산출". 감사 `docs/dumping-ux-audit-2026-09-05-r5.md`, 검토서 9절. **7라운드 UI(2026-09-06, 검토서 11절)**: 지도 칩은 지도 위가 아니라 지도 위쪽 띠 `MapToolbar`, 지도 위 오버레이는 범례 카드·후보 목록뿐(`MapOverlays`, map-controls.tsx에 기본 export 없음). 데스크톱 패널 폭은 `use-sidebar-width.ts`(40%, 드래그). 값 0인 격자는 옅은 테두리로 그리고 범례가 "흰 바탕 = 세 값 모두 0"을 말한다. 결재용 한 장은 결재란 없이 제안별 이유(`reasonSentences`) 블록. 괄호 문구는 `joinParen`. 정책 탭 04·05는 `<details>`. 글자 최소 12.5px, 본문 15px. ⚠️`npx`도 rtk 훅이 pnpm으로 바꿔 node_modules를 부순다(2026-09-06 실사고) → 도구는 `node node_modules/.bin/<도구>`. **9라운드 출품 재검토 반영(2026-09-13, 검토서 [docs/dumping-submission-review-2026-09-13.md](docs/dumping-submission-review-2026-09-13.md) + contest-review 9절)**: ★지오코딩 폴백 더미 — 카카오가 주소를 못 풀면 `legalDong` 없이 구 중심 한 점(37.5384,127.0845)을 주고, 그 점이 구의1동 격자 963200_1948800에 민원 67·과태료 123·대장 1,089동을 쌓아 핫스팟·집중관리 1위·회귀 최다 셀이었다. 비공개 레포 geocode 2종·ledger·kapt가 `legalDong` 없는 결과에 격자 미부여(민원 행은 유지, `geocode_quality.json` → `meta.geocode` → 방법 모달 "품질" 표). 재적합 β +0.312→+0.306(export가 DB Covariate·β 엣지를 `base100`으로 덮고 원값은 `coefficient_initial`/`beta_initial`), 집중관리 32(앱 제외 11), 백테스트 63.1%/9.0%, 지도 959셀, verify EXPECT 959·120,625·42,292, 해시 116. 고아 파생물 3종(viz·cell_complaints·cell_infra) 생성 스크립트 `build_cell_layers.py` 복원(현행 입력 일치 검증 후 적용), 품목 규칙은 `waste_category.py` 한 곳. `regression_v2.json.itemSplit`(생활쓰레기 β +0.295 유지·차량꽁초 +0.025 비유의, 골목·간선 음수는 꽁초 쪽) → 카드 "품목 분리"·`ev-item-split`. `hotspots.backtest.baselines`(누적/최근90/반감기동일가중 9.3/7.8/9.4% vs 현행 9.0% 동급) · `fines.byRoute.complaintLink`(신고 유래 과태료 중 ±3일 같은 격자 민원 8.1%) · `facts.appStep`(앱 2026-03 42→146 계단, 원인 미확인) · `slaShift`·`binRecoOverlap`·`geocodeExcluded`. 발견 카드 17장(품목 분리·자료 정정·처리 지연 추가), 카피 게이트 FORBIDDEN에 `발생이 아니라 신고 창구|연관이 없(었)?습니다|연관이 없다|연관이 없고|원인 쪽|대부분 앱 보급 효과`, 첫 화면 결론 "단속에 잡히는 무단투기는…발생이 늘었다고 읽기 어렵습니다". 이벤트 스터디·대칭 DID 수치는 export가 `event_study.json`·`did_symmetric.json`에서 철회 노드/엣지에 붙임(하드코딩 0). 그래프 105노드 169엣지. ★그래프 관계 추가 시 [lib/dumping/schema.ts](lib/dumping/schema.ts) 도메인/레인지 확인(contradicts는 Evidence→Claim·Covariate). 남은 일: 대회 확정, 청소과 확인(2026-03 앱 급증 원인·단속 인시), 안전신문고 건수, 조치 대장 실등록, 시크릿 교체. **음성 질의응답(2026-09-15, 구청장 시연·청장실 대시보드 전 단계)**: 프롬프트 "답변 형식"이 답을 1부 "말로 하는 답"(3~5문장, 통계용어·소수점 금지) + `[부연]` 구분줄 + 2부 근거 불릿으로 내게 한다. 분리·문장 단위·TTS 정리는 [lib/dumping/answer-parts.ts](lib/dumping/answer-parts.ts)(`splitAnswer`·`completeSentences`·`ttsClean`, 테스트 `tests/dumping-answer-parts.test.ts`). 화면은 1부 크게·2부 "근거 수치와 한계" 옅게. 입력은 Web Speech(ko-KR, 크롬·엣지·HTTPS) [components/dumping/use-voice.ts](components/dumping/use-voice.ts) `useSpeechInput`, 출력은 `/api/dumping/tts`(Gemini `gemini-3.1-flash-tts-preview`, 목소리 `Kore`, env `GEMINI_TTS_MODEL/VOICE`, 같은 GEMINI_API_KEY, 레이트리밋 `dumpingTts` 60/분) PCM 16bit·24kHz 스트림을 `useSpeaker`의 Web Audio 큐가 문장 단위로 이어 붙인다. ★문장 단위로 부르는 이유: 세 문장 한 번에 11초, 한 문장 스트리밍 첫 소리 1초(실측). 브라우저 speechSynthesis는 서버 실패 시 대체 음성뿐(크롬 윈도 기본 한국어는 기계음). 마이크로 물으면 자동 읽기가 켜지고(`voiceOn`), 시드·이전 답에는 "듣기" 버튼. **호출어 "김주임"**(`useWakeWord`, `WAKE_WORD`·`WAKE_VARIANTS` 상수): continuous 인식기 하나를 켜 두고 결과에서 호출어를 찾아 알림음 뒤 8초 안 발화(또는 호출어에 이어 붙은 말)를 질문으로. 크롬이 끊으면 onend에서 재시작, TTS 재생 중(`muted`)엔 결과 폐기, 켜 둔 동안 누르고 말하기 버튼은 잠금(인식기는 페이지당 하나). 상시 오디오가 구글로 가므로 청장실 상시 설치면 온디바이스 엔진(Porcupine)으로 감지부 교체 검토. 이 PC 샌드박스에서 Turbopack `next dev`는 PostCSS 자식 프로세스 0xc0000142로 페이지 렌더 실패 → **`next dev --webpack`이면 렌더됨**, 스크린샷은 ehojo-auto의 playwright + `%LOCALAPPDATA%\ms-playwright\chromium-1228` 실행파일 직접 지정. **시연 다듬기(같은 날)**: ★윈도 폰트 이상의 원인은 `font-mono` 스택(Consolas 등)에 한글이 없어 "배·곳·건"이 굴림으로 떨어진 것 → globals.css `.crowd-page`에서 `--font-mono`·`.font-mono`에 SUIT 폴백. 등장 애니메이션 `dump-rise`(+`--i` 지연)·`dump-line`, 히어로 수치 `CountUp`(policy-board), 생각 중 `ThinkingIndicator`(스트리밍 캐럿 `dump-caret`), 청취 패널(마이크 링 `dump-ring`+`useMicLevel` 음량 막대+큰 받아쓰기 글자), 읽는 중 `dump-eq`. 지도 "동별 막대" 칩(`MapView.dongBars`) = 행정동 15곳 민원·과태료 등축 3D SVG 막대(`dongBarSvg`, 외곽선 꼭짓점 평균 위치, `dump-grow`). **10라운드(2026-09-15, 검토 보고서 [docs/dumping-production-review-2026-09-15.md](docs/dumping-production-review-2026-09-15.md) + 코덱스 리뷰 3편 합본 반영)**: ★**정오표가 노드 라벨도 덮는다** — [lib/dumping/errata.ts](lib/dumping/errata.ts) `NODE_ERRATA`(ERR-002~011: claim-mgmt "관리주체 부재의 함수"·claim-bias "발생 증가가 아니라 신고편향"·ev-channel "대부분이 신고편향"·claim-not-concealment·kpi-dump-rate "발생률"·ev-dong-rate·레버 "(무예산)"·ev-seoul-app-trend 쉬운 설명 "같은 착시")를 현행 결론으로 바꾸고 원문은 `props.label_initial`·`erratum`에 보존. 데이터 라우트·프롬프트가 같은 그래프를 보고(프롬프트 직렬화는 `label_initial` 제외), 근거 그래프 상세 카드가 "정정 ERR-n" 배지를 단다. 원본 graph.json은 재현 해시 대상이라 불변. 카피 게이트 FORBIDDEN이 정오표 후 그래프·프롬프트(`FORBIDDEN_PROMPT`는 인용 4어 제외)에도 걸린다. Claim 타입 라벨은 "주장"(상태는 배지). **물어보기 규격**: 프롬프트 "답변 형식"이 1부(결론 30자·이유 40자 이내·한계는 효과/원인/비교 질문에만·"다만" 1회·자평/상투구/새 용어 금지·기록≠발생)+`[부연]`+2부 슬롯 불릿(`- 수치:`/`- 근거:`/`- 한계:`/`- 다음 행동:`, 45자, 1부 반복 금지, "허용 출처" 목록 밖 문서명 금지). 프롬프트에 "제안 6건"(`proposalRows`) 정본·"이동식 CCTV 재배치 후보 N곳"·세 목록(후보 20≠핫스팟 20≠집중관리 32) 구분 규칙·허용 출처(Dataset 노드 라벨+발견 카드 이름) 신설. 화면은 [answer-parts.ts](lib/dumping/answer-parts.ts) `sentencesOf`(1부 문장별 줄, 첫 문장 ≤45자만 굵게)·`detailLines`(슬롯 칩)로 그리고, 시드 [qa-seeds.ts](components/dumping/qa-seeds.ts)는 17개 전부 같은 규격(`answer`=1부·`detail`=2부, `core` 6개만 기본 노출 + "더 보기", "예산은 얼마나 드나"·"대책 효과는 언제 확인되나" 신설). 중단 답은 `aborted`로 재사용 제외, 생각 중 문구는 "답을 만드는 중 · n초"(연출 단계 문구 제거). 평가셋 `dumping-qa-eval.mjs`에 decide·budget·cctv-count·decide-repeat 문항과 형식 검사(`formatCheck`: 길이·슬롯·중복·허용 출처·상투구, 판정엔 미포함) 추가. **정책 탭**: 결론 문장에 앱 제외 신고·순찰 적발 배율 병기, 타일 = β / 채널고정 민원(앱 제외) / 집중관리 상습격자(앱 제외, 분기 추이), 결론 아래 제안 6건 칩, 02 "왜 이런 제안인가"는 `Folded`, 근거 경로 "한계"는 방법 모달(해설서 링크는 모달 하단). 비용 배지 "무예산"→"추가 예산 없음"(`COST_ORDER`), `reasonSentences`에서 p값 풀이·"지금 바로 조정" 제거. 결재용 한 장 = 배경(3일 내 처리율·신축 세대)+전제(조치 대장 담당)+항목별 담당/추가 지출/개시/판정 4칸(`PolicyPrintModal data` prop). **발견 탭**: 태그 "가장 강한 연관·신고 채널 분해·효과 철회·대책 공백"(`FINDING_GROUPS` 결론 5·검증 8·한계 4, 결론만 펼침). 화면 문장은 "실제 발생"·"전체 발생" 대신 "기록". 방법 모달 시설 수는 "장부 N(지도 M)" 병기, 강건성 β 정정 전 표본 고지, 80% 구간 "같은 잔차·독립 검증 전". 운영 탭 CCTV 순위 "보고 N개 구 중", 조치 대장 문구에서 파일 경로 제거. 지도 기본 원은 과태료(`DEFAULT_VIEW`·`MODE_MAP.overlay`), 등장 애니메이션은 1.5초 뒤 `dump-anim-off`로 1회만, 390은 지도 접힘 시작(`mapCollapsed`, viz 반영 시 펼침). p값 풀이는 "연관이 없다고 가정했을 때 이만한 차이가 나올 가능성"으로 전면 교체. 남은 일: 강건성 검정 정정 후 재적합(비공개 레포), 탭 순서(물어보기 위치)·"김주임" 호출어는 사용자 결정. **11라운드(2026-09-15 밤, UI 가독성)**: 동별 막대 툴팁을 지표별 색띠 블록 카드로(★leaflet 기본 `white-space:nowrap`이 각주를 카드 밖으로 밀던 원인 → `.dump-bartip-wrap`에 `normal`), 정책 탭 제안 6건 칩 흐름→번호·이름·예산 3열 목록(03 카드에 같은 번호, 라벨 꼬리가 예산 등급과 같으면 제거), 첫 화면 수치 칸 3개 클릭→지도(`HeadlineId`: β=다가구·단독 바탕, 채널고정=민원 바탕(격자에 채널 구분이 없어 "앱 포함 전체" 라벨), 상습격자=`showCritical` 토글을 운영·전망 탭과 공유, policy 탭에서도 강조 레이어 표시), 발견 카드 위계(결론 문장 먼저·근거 문장 2줄 클램프), 운영 07 구조 전망 문장→타일 3+열 정렬. 담당 문구는 `splitParen` 규약으로 괄호 앞=카드용 짧은 이름, 괄호 안=모달 "담당 구분" 줄·결재 한 장에 펼치는 역할 근거(정본은 비공개 레포 `export_dashboard.py` `LEVER_EXEC`; 공동배출시설은 폐기물관리법 15조·광진구 폐기물관리 조례 8·9·10조 실측으로 청소과 주무·건축과는 신축 준공 전 보관시설 확인). ★로컬 재빌드 전 `pkill -f next-server`(`next start` 패턴은 자식 프로세스를 못 잡음), Playwright `button[aria-pressed]`는 지도 툴바 칩도 잡히니 `aside button` + hasText. **12라운드(2026-09-16, 정책 탭 리팩토링·음성)**: 정책 탭 첫 화면=결론 2문장을 번호로 분리 + 제안 6건 카드(수치 칸 3개는 발견 카드·운영 성과지표와 겹쳐 후속으로 제거, 칸→지도 배선 `HeadlineId`도 함께 삭제. 항목 제목 01·02는 23px, 접힌 03·04·05는 18px)(번호·이름·예산 / 기대·담당·검증 세 줄, `expectedEffect`=판정 엣지 방향 lowers→감소·stabilizes→안정 + 겨냥 요인, 효과 크기 단정 금지). 제안 목차·"왜 이런 제안인가"·결재용 한 장(`policy-table.tsx` 삭제, `requestSentence`는 lever-view로 이관)은 같은 제안이 세 번 나오던 원인이라 제거. 섹션 번호 01 결론·02 제안·03 기존 수단·04 성과지표·05 원칙. 문구 규칙: 제안을 "사업"이라 부르지 않는다(모달 `noun`=제안/수단, 카피 게이트 테스트). "노출" 풀이는 발견 모달 쉬운 풀이·카드 태그 title·방법 모달·대비 보드에. 음성: 호출어 상태기계를 `lib/dumping/wake.ts` 순수 함수로(★호출어만 부른 구간의 최종 결과가 빈 질문으로 제출돼 idle로 돌아가던 것이 "두 번째부터 반응 없음"의 원인. hold로 유지), TTS `TtsPlayer.stop()`이 onended로 live를 음수로 만들어 speaking이 굳던 것 수정(onended null·utterance 참조 유지+시간 상한), 받아쓰기 뒷손질 `lib/dumping/stt.ts`(수고→수거, 무단 투기·중곡 삼동 등 도메인 띄어쓰기, 질문이면 `?`·명령형은 그대로), 답변 중 호출어 질문은 입력창에 남기고 알림. **후속(같은 날)**: 제안의 "기대"는 수치 방향이 아니라 **가정한 작동 원리**(예: 수거 시간대 조정=방치 시간 단축). 정본은 비공개 `export_dashboard.py` `LEVER_MECHANISM`(칩 ≤10자 + 가정·조치·기대 세 슬롯 각 ≤45자, 테스트 게이트) → 노드 props `mechanism`/`mechanism_assumption`/`mechanism_action`/`mechanism_expect` → 카드 칩+가정·조치·기대 세 줄, 모달 "가정한 작동 원리" 블록, 물어보기 프롬프트 제안 줄("가정: … 조치: … 기대: …"). 모달: 흐름도([제안]→겨냥→…) 삭제(화살표 뜻이 안 읽힘), 겨냥 조건 막대만 먼저·전체 비교는 `<details>`, "분석 메모"(원문 rationale, 정정 전 β·금지어) 비노출, "담당 구분"→"담당 상세". 물어보기: Esc가 듣기 취소(제출 없이 abort)·깨움 해제·답 생성 중단·읽기 멈춤(`useSpeechInput.cancel`·`useWakeWord.dismiss`), 생각 중 표시는 사실 단계 둘+경과 초: ★서버(`ask/route.ts`)가 스트림을 먼저 열어 접수 표시 `ASK_ACCEPT`(제로폭 공백)를 즉시 보내고 그 뒤 모델을 부른다(응답 머리를 모델 연결 뒤에 보내면 "보내는 중"에서 답으로 건너뛰던 실사고). 모델 호출 실패는 스트림 안 `ASK_ERR`(NUL+"ERR:")로. 규약은 `lib/dumping/answer-parts.ts`. 실측: 접수 0.4초, 첫 문장 11초. **13라운드(2026-09-16, 3D·이유·날씨)**: export(비공개) 추가 3종 = 핫스팟 행 뒤 5칸(12개월·최근90·이전90·마지막 기록 경과일·집중관리 여부), `dong[].ch`(앱·120·직접)·`dong[].yr`(연도별 민원 접수일·과태료 위반일), `env.cellWeather`(grid 순서 [더움·온화·추움·비] 민원)+`weatherDays`. 지도: 상습격자 32 기둥(높이=12개월 건수, 정책 04 성과지표 토글에서도), 예측 핫스팟 20 기둥(높이=점수, 순위 라벨, 줌아웃은 점), 격자 기둥 토글(원 지표 5건+ 칸, 237칸), 동별 막대 모드(합계·채널 스택·연도 버튼), 날씨별 원(그 조건 민원 하루당 환산, 보통 원 대체, 시드 "계절이나 날씨" viz). 상수는 `lib/dumping/labels.ts`(★map-controls↔dumping-map 순환 import 회피). 등축 헬퍼 `isoBar`/`columnSvg`/`cellBarsSvg`. 운영 02 핫스팟 목록에 "이유" 줄(최근 90일 vs 이전 90일·12개월·마지막 기록·집중관리 칩). 항목 제목 위계(`section-head.tsx` SectionHead 23px·번호 15px, Folded 18px)를 정책·발견·운영·물어보기 전 탭에 적용. 법령 인용 팝오버 `law-ref.tsx`+`lib/dumping/law.ts`. **답변 속도(같은 날)**: 실측(프롬프트 3.3만 토큰) 기본 첫 토큰 8~10s(사고 1.3~1.7K토큰)·low 6s·minimal 2.4s·medium≈기본·high 16s. 평가셋 11문항(scripts/dumping-qa-eval.mjs, ★답이 빨라져 분당 10회 한도 429→Retry-After 대기 추가, 접수 표시·ERR 규약 처리)으로 low/minimal은 **1부 길이 규격 위반 급증(형식 이상 2→9건)**, 질문 뒤 형식 리마인더(`FORMAT_REMINDER`)로 형식은 회복되나 내용 게이트 누락(후보 20곳·예산 미산정)과 오탈자("자원 배구")가 남아 **기각**. 채택: ①프롬프트 다이어트(온톨로지 직렬화에서 derived_by·asof·confidence·refit·variable 등 제거, 속성 없는 관계는 (from,관계)별 묶음. 54K→48K자, 캐시 토큰 32.6K→30.0K, 게이트 11/11·형식 이상 3=기본과 동급) ②Gemini 컨텍스트 캐시(`cachedContents`, displayName=프롬프트 해시, TTL 1h, list로 재사용, 실패 시 인라인 폴백, `GEMINI_PROMPT_CACHE=off`) ③준비된 답 즉답(`lib/dumping/seed-match.ts` 2-gram Dice ≥0.55·2위와 0.12 차, 조사·어미 정규화, 평가셋 질문 오탐 0, 화면에 "준비된 답" 배지+"모델에게 새로 묻기") ④사고 수준은 env `GEMINI_THINKING_LEVEL`(기본 비움=모델 기본). **목소리 고르기**: `lib/dumping/voices.ts` 프리셋 10종(기본 Kore), TTS 라우트 `voice` 인자(목록 밖이면 기본), `useSpeaker().setVoice`가 localStorage `dump-tts-voice`에 저장, 물어보기 툴바 "목소리" 버튼→칩 누르면 한 문장 미리 듣기. **14라운드(2026-09-16, 시연·공모전 대비 프로덕션 리뷰, 검토서 [docs/dumping-production-review-2026-09-16.md](docs/dumping-production-review-2026-09-16.md) + 냉독 2인 원문·평가셋 prod/local2)**: ★★음성이 프로덕션에서 한 번도 안 됐다: `next.config.mjs` 전역 `Permissions-Policy: microphone=()`(2026-03)가 /dumping에도 걸려 `SpeechRecognition`이 `not-allowed`, `getUserMedia` 거부(Playwright 가짜 마이크 실측, 헤더 제거 대조군으로 확정). `/dumping/:path*` 항목에 `microphone=(self)`(같은 키는 뒤 항목이 이긴다), `tests/dumping-headers.test.ts`로 고정. ★암호화한 `bin-recos.json`이 `lib/dumping/bin-recos.ts` 정적 import로 공개 청크에 평문 번들됐다 → 파일 삭제, 데이터 라우트 `bin-recos` 추가, 대시보드가 로그인 뒤 받아 `map.binRecos`(옵셔널)로 전달. 데이터 라우트는 인증 먼저·`Object.hasOwn`. 물어보기: "모델에게 새로 묻기"가 캐시 검사에 잡혀 무동작이던 것(force면 캐시 건너뜀·옛 카드 제거), 빈 스트림 답 `aborted`, 중단 시 `speaker.stop()`, `seed-match.ts` POLARITY 가드(부정·반대 낱말이 한쪽에만 있으면 안 붙임. "놓으면 안 되나"가 "놓아야 하나"에 0.63으로 붙던 실측), 프롬프트 동별 "채널"은 기간 전체 합계임을 명시(모델이 "2026년 234건(앱 298건)"을 냈다)·부분 연도는 "2026년(1~8월)" 표기, "결재용 한 장" 잔존 인용 제거+게이트. 모바일: 접힌 지도(높이 0)에 `fitBounds`가 줌 18로 굳던 것 → `pendingFitRef`로 높이 생길 때 실행, 핫스팟 클릭·상습격자 토글이 지도를 편다. `use-sidebar-width` localStorage try/catch. 툴바 md 이상 `flex-wrap`. 연도 칩은 민원 연도만. 숫자·문구 정합: 의류수거함 475(tallyInfra)·대비 보드 "+5.4%" 삭제(정정 전 표본 41,633과 비교한 옛 값)·"10건 이상"·`pLabel`("p0.012" 수정)·R² 카드 수치·정거장 857 설명·후보 "전 기간"·계절 2자리·띄어쓰기 `{" "}`·"발생률"→"천명당"·"집계 중인 달". 테스트 234. 평가셋은 같은 코드에서 prod 8/11·local 10/11(온도 변동, 실패는 표현이 정규식을 비껴간 것). 냉독: 비서실장 "보완 후 가능"(결재 항목 부재), 심사위원 40/50 "보완 후 출품"(결론 vs 제안 2·4·5 서사 모순은 사용자 결정). ★재현 도구: Playwright `aside button[aria-pressed]`는 숨은 물어보기 마이크 버튼이 먼저 잡힘 → `:visible`, 패널 스크롤 컨테이너도 `aside .overflow-y-auto` 중 `clientHeight>0`인 것. rtk 훅이 grep을 rg로 바꿔 `\|` 대체 정규식이 깨진다 → `grep -F`. 남은 일: 비밀번호 4자리는 사용자가 유지 결정(2026-09-16). 커밋 599fe74 main 푸시. **14라운드 후속(같은 날)**: 제안 2·4·5 `targetNote`(lever-view, 카드 "겨냥" 줄·모달·프롬프트 공용, 사람 요인 제안만)·운영 01/정책 04 타일 큰 숫자=앱 제외 11·프롬프트 "사업" 금지·평가셋 budget 정규식 완화. 근거 그래프 배치 4종 [components/dumping/onto-layouts.ts](components/dumping/onto-layouts.ts)(구면·층별 흐름(스기야마 약식, 열 머리 종류 이름, `fitZoom`)·분류별 군집·선택 중심(고른 노드 따라 재배치), 평면 배치는 드래그=이동·라벨 13.5 고정, 테스트 5). 통계 방법 탭은 [components/dumping/methods-stats.tsx](components/dumping/methods-stats.tsx) 카드 8장(질문→도식 7종→쉽게 말하면→결과 수치 칸→✓검증→△한계→해설서 앵커, 수치는 facts). "이 분석"→"이번 분석" 24곳(조사 '이' 제외). 지도 원 반경 상한 48m(제 칸 안)+26건 이상은 채움 진하기. ★Leaflet `_leaflet_pos` 오류(그래프 탭 전환 시): 원인은 데이터 도착 직후 `fitBounds` 줌 애니메이션 중 언마운트 → 250ms 뒤 `_onZoomTransitionEnd`가 제거된 지도를 만짐. 초기 맞춤 `animate:false` + draw 효과 `await import` 뒤 `mapRef.current !== map` 가드 10곳 + RO `isConnected` 가드. 재현은 스크래치패드 err-probe2(전환 지연 0ms). 추천 시각화 검토(검토서 11.6): 1순위 격자 산점도(다가구·단독 vs 과태료, 칸 1,062), 2순위 β 계수 플롯(모형별), 그다음 채널 슬로프·처분 Sankey·월×연 히트맵. 핫스팟 bump chart·β 신뢰구간은 export 필요. **프리미엄 에디토리얼 디자인(2026-09-17)**: 종이·잉크·단일 액센트(#0c6155), 그라데이션·컬러바 없음. 토큰은 globals.css `.crowd-page.crowd-light.dump-page` 스코프(/crowd 무영향): 바탕 `--dump-paper` #f4f1ea·카드 `--dump-paper-white` #fffdf9·잉크 #171a21·헤어라인 rgba(23,26,33,.13). 숫자·연도·항목 번호(`.font-mono`, `.dump-idx` 이탤릭, `.dump-meta-v`)는 Fraunces 세리프(app/dumping/page.tsx `next/font/google`, axes opsz·SOFT, `--font-fraunces`, 셀프호스팅), 한글은 SUIT. 헤더 [components/dumping/dump-mark.tsx](components/dumping/dump-mark.tsx) 자체 마크(액센트 단색 사각 안에 종이색 칸 하나+원 하나. 선·격자 없음. 광진구 공식 표장 미사용)+워드마크+꼬리표(`.dump-kicker` 자간 .14em)+한 문장 부제+기준일·민원·과태료 세리프 수치(lg 이상)+둥근 아웃라인 버튼. ★헤더 질감 금지: 점무늬 격자를 깔았다가 그래프용지처럼 촌스럽다는 피드백으로 뺐다(2026-09-17). 종이 바탕+헤어라인만. 탭은 상자→밑줄(`.dump-tab::after` 액센트 선). 로그인 게이트도 마크·워드마크. 모달 종이 카드+잉크 그림자+배경 잉크 틴트 블러. 하드코딩 `bg-white` 19곳→`bg-[var(--cp-panel)]`(dumping-map 툴팁 HTML 제외). ★Playwright는 `[role=tab]:has-text`·`aside button:visible` 셀렉터 그대로 유효(구조 불변). **그래프 라벨 가독성(2026-09-17)**: 노드를 고르면 무관한 라벨은 숨기고 이웃 라벨은 포커스에서 바깥쪽(옆·위·아래) 자리로, 자리 충돌 시 `onto-view.ts placeLabels`가 대안 자리(alts) 순서로 옮기고 전부 겹치면 숨김. 관계 라벨은 선 58% 지점 옆(9px 비킴)에 두고 노드 라벨보다 뒤에 자리 잡음. 선택 중심 배치는 고른 게 없어도 허브 기준 에고 뷰(focus=HUB), 맞춤 줌은 가운데+2단계 이웃까지만(`radialFit`, 전체 맞춤은 콩알). 고리 반지름은 노드당 17px 둘레 확보. 고른 노드 라벨 34자. **지도 전면 디자인(2026-09-18, 4안+2안 조합)**: 지도(근거 그래프 탭은 그래프)가 화면 전체를 채우고 유리 패널(`.dump-fl` 백색 88%+블러, `prefers-reduced-transparency`면 불투명)이 뜬다. 상단 띠 = 마크 알약(누르면 초기화)·탭 알약(`role=tab`, 선택=잉크 바탕 `.dump-tab`)·데이터·방법. 왼쪽 카드 `<aside>` = 탭 내용 전부(전 탭 공통, 폭 `use-sidebar-width` 기본 440px 드래그, 카드 바닥에 "김주임에게 물어보기" 지름길+한계 고지). 오른쪽 열(md+, 지도일 때) = [map-controls.tsx](components/dumping/map-controls.tsx) `MapLayerPanel`(바탕 라디오·원 겹치기·보기·시설 세로 목록, 예전 툴바 대체)·`CandidateList`·`MapLegend`(축약, 원·빈 칸 설명은 "자세한 설명" 안) 한 열이라 겹침 없음, 바닥 84px는 Leaflet 줌 버튼 자리. 아래 띠(xl+) = [timeline-strip.tsx](components/dumping/timeline-strip.tsx) 월별 민원(`appStep` 이후 달 액센트). 모바일 = 카드가 하단 시트(`--dump-sheet-top` 기본 44dvh, 손잡이 드래그는 `useSplitPane` 기본 부호, "지도 접기"=104px), 레이어·범례·후보는 "레이어" 버튼 덮개. ★구 전체 맞춤은 `DumpingMap fitPadding`(카드·열·띠가 가리는 만큼 `paddingTopLeft/BottomRight`, `useBreakpoint` md/xl로 계산) — 없으면 구가 카드 뒤에 숨는다. 근거 그래프는 제 툴바가 있어 컨테이너를 카드 오른쪽·상단 띠 아래로 오프셋. 토큰: 잉크 #14201c·액센트 #c2410c(진한 #9a3412) 하나(옛 teal #0c6155 122곳 기계 치환, PAL_GREEN 끝색만 #0b4f45 유지)·지도 바탕 `--dump-ground` #e8ebe6·과태료 램프 주황(PAL_AMBER). 폰트: 수치 Manrope(`--font-manrope`, `.font-mono`·`.dump-idx`·`.dump-meta-v`), 결론 머리기사 Noto Serif KR(`.dump-headline`), Fraunces 제거. 정책 탭 = 결론 머리기사+부제+핵심 수치 4칸(민원·과태료·β·집중관리, facts 파생)+제안 6건 한 줄 목록(`ProposalRow`: 번호·이름·기대·담당·예산, 가정·조치·검증은 모달)+접힌 02 기존 수단·03 성과지표+04 원칙(번호가 01 제안부터). 물어보기 모드 토글(호출어·소리·목소리)은 입력 알약 밖 둘째 줄. ★Playwright: `[role=tab]:has-text` 유효, 레이어 줄은 `button[aria-pressed]`(툴바 칩 없음), 모바일 레이어는 `button:has-text("레이어")`. **16라운드 지도 입체화(2026-09-19, Leaflet→MapLibre GL, 계획 `.claude/plans/2026-09-19-dumping-map3d.md`)**: 서울 3D Atlas(Three.js 미니어처)를 보고 데이터 뜻이 있는 것만 가져왔다(기울인 시점·진짜 입체 기둥·OSM 건물 3D·아차산 지형·평면/입체 토글·자동 회전. 낮/노을/야경·비/눈·나무·배·차는 시간대 데이터가 없어 장식이라 버림). 바탕은 **외부 타일 서버 0**: [lib/dumping/basemap-style.ts](lib/dumping/basemap-style.ts)가 `@protomaps/basemaps` light 플레이버를 종이 톤으로 덮고(아이콘 레이어·구 안 OSM 동 라벨 `within` 제거) `public/dumping/basemap/`의 정적 파일만 쓴다 = `gwangjin.pmtiles`(Protomaps 20260918 빌드 bbox 127.02,37.49,127.16,37.60 z≤15 · 10.2MB, `pmtiles extract`) · `dem.pmtiles`(AWS terrarium z8~14 · 6.4MB, mbtiles→convert) · `fonts/`(Noto Sans 라틴 0-511만, 한글은 `localIdeographFontFamily`=SUIT). ★`public/` pmtiles는 HTTP Range 필수(`next start` 206 실측, python http.server는 안 됨). [components/dumping/dumping-map.tsx](components/dumping/dumping-map.tsx)는 소스·레이어를 처음 한 번 빈 데이터로 선언하고 setData·setPaintProperty만 바꾼다(그리는 순서: 마스크→격자 면→동 채움→원→노선→상습격자 면→격자선·동 외곽선·구 경계→건물→시설·후보·배치추천→기둥 3종→라벨). 순수 계산부(팔레트·BASE_DEF·CIRCLE_DEF·툴팁·GeoJSON 조립·동별 막대 SVG)는 [components/dumping/map-geo.ts](components/dumping/map-geo.ts)(★dumping-map은 CSS를 임포트해 tsx 테스트가 못 읽는다. map-controls·테스트는 map-geo에서 가져온다). 기둥 = fill-extrusion(격자 기둥은 칸 60% 폭, 지표 둘이면 좌우 두 기둥, 5건 이상 칸만 · 상습격자 · 핫스팟 72m 정사각, 높이 18~280m `colHeight`), 순위·값 라벨은 바닥 심볼. 원 반경은 미터(`radiusMetersExpr` 지수 보간). 동별 막대는 HTML 마커(SVG 유지, 켜면 지도 동 라벨 숨김). `MapView.tilt`(기본 켬, 기울기 55·방위 -18, 건물·지형은 입체에서만)·`orbit`(4도/초, 만지면 `onOrbitStop`), 격자 기둥 켜면(패널·viz 모두) 입체 자동. ★구 전체 맞춤: fitBounds는 기울기를 모르고 옵션 padding은 수평 기준 픽셀 이동이라 기울이면 어긋난다(모바일 시트 뒤로 구가 숨던 실측) → `map.setPadding(fitPadding)` 뒤 `cameraForBounds({bearing})`+`easeTo({pitch})`, 기울기면 줌 -0.3(`TILT_ZOOM_BACK`). ★`cameraForBounds`에 `maxZoom: undefined`를 넘기면 extend가 기본값을 덮어 NaN(에러 화면). ★훅 순서: 대시보드의 `stopOrbit` useCallback은 로그인 게이트 분기 위에(React #310 실사고). 오른쪽 열 바닥 여유 140px(줌·나침반·출처 (i)), 출처는 hover에만 펼침(CSS). 툴팁은 maplibre Popup 하나를 `queryRenderedFeatures` 최상위 피처의 `tip` 속성으로. 테스트 `tests/dumping-map-geo.test.ts`(261). Leaflet은 crowd·facility·map-view가 계속 쓴다. **17라운드(2026-09-19, sunlight-fund 테마·동별 기둥·드론 비행)**: 디자인 토큰을 햇빛소득 시뮬레이터(~/workspace/sunlight-fund app/globals.css)에서 가져왔다 = 도면지 종이(#f1ecdf, 지도 바탕 #e9e3d3)·잉크 #1c1a15·햇빛 앰버 액센트 `--dump-accent` #c0741a(진한 `--dump-accent-ink` #8a5210, 워시 `--dump-accent-wash`)·글꼴 고운바탕(결론 `.dump-headline`, `--font-batang`)+IBM Plex Sans KR(본문·숫자 `--font-plex`, 숫자 강조는 굵기 600)·유리 패널 `.dump-fl`=sun-fl 절제판(radial 하이라이트+blur 24+inset 하이라이트)·**라이트/다크** [components/dumping/theme.tsx](components/dumping/theme.tsx)(`ThemeSwitch` 유리 스위치, `useTheme`, html[data-theme], localStorage `dump-theme`, 기본 라이트, View Transition 원형 확산, 초기 스크립트는 app/dumping/page.tsx). 액센트 하드코딩 `[#c2410c]`·`[#9a3412]`는 Tailwind v4 `(--dump-accent)`·`(--dump-accent-ink)`로 전부 치환(SVG·style은 `var(--dump-accent)`, maplibre 안 색은 hex 그대로). 데이터 색(램프·원·기둥)은 테마가 아니라 지도 문법이라 불변. 지도 다크 = `buildBasemapStyle(ring, "dark")`(Protomaps dark 플레이버+밤 톤 `NIGHT`) → DumpingMap `theme` prop이 바뀌면 현재 스타일에서 `dump-` 소스(데이터 포함)·레이어(paint 포함)를 새 바탕에 옮겨 실어 `setStyle(diff:false)`, `style.load`에서 `applyThemePaint`(마스크·건물·경계·라벨 후광)+지형 재적용. ★지형을 켠 채 setStyle하면 `shaderPreludeCode` 오류 → 전환 전 `setTerrain(null)`. ★스타일 교체로 사라지는 이미지는 `styleimagemissing`에서 재등록. **동별 막대→진짜 기둥**: `map-geo.dongColumnsFC`(동 가운데 민원 파랑·과태료 갈색 정사각 96m 두 기둥, 간격 70m, 높이 24~720m, 채널 모드는 base/h 토막 스택, 라벨 "동 이름\n민원 · 과태료", 툴팁은 `card:1`이면 `.dump-bartip-wrap` 카드), SVG 등축·HTML 마커·`.dump-bar3d` CSS 삭제. **드론 비행** `MapView.fly`(패널 "드론 비행", 입체에서만, 자동 회전과 배타, 만지면 `onOrbitStop`이 둘 다 끔): `map-geo.flyTour` = 구 전체(pitch 50) → 핫스팟 상위 5곳(줌 15.7·pitch 62·방위 +48°씩, 8초 이동·2.5초 머묾) → 구 전체(방위 한 바퀴), `easeTo` 이어 붙이기(moveend→setTimeout), 정리에서 `map.stop()`. **건물 전수(GIS건물통합정보, 적용 완료)**: 자료는 브이월드 데이터마켓 https://www.vworld.kr/dtmk/dtmk_ntads_s002.do?svcCde=NA&dsId=18 (★국가공간정보포털 nsdi.go.kr은 2024-01 폐쇄·이관. 로그인 후 서울특별시 전체데이터 SHP `AL_D010_11_YYYYMMDD` 695,754동·dbf 1.2GB, EPSG:5186, CP949, 필드 A0~A28: A3 법정동코드·A16 높이·A24 건물명·A26 지상층수) → [scripts/dumping-buildings.mjs](scripts/dumping-buildings.mjs)(ogr2ogr `A3 LIKE '11215%'`로 광진만 27,491동 → tippecanoe z13~16 → `public/dumping/basemap/buildings.pmtiles` 3.6MB, 기준일 2026-09-09). `basemap-style.ts HAS_NSDI_BUILDINGS=true`: 구 안은 이 소스(높이 0이면 층수×3.2m), OSM 건물은 구 밖만(`within` 필터). 갱신은 새 SHP로 스크립트 재실행. 테스트 263. **18라운드(2026-09-19, 입체 문법·유리·시연)**: maplibre는 지형이 켜지면 fill·line·circle을 지형 텍스처로 굽고(건물 아래 깔림) fill-extrusion·symbol만 3D로 그린다 → 입체에서 ①**건물을 격자 칸 값으로 칠한다**(`basemap-style.ts` NSDI 소스 `promoteId:"id"`(UFID 문자열), `dumping-map` `joinBuildings`가 `sourcedata`/`idle`마다 `querySourceFeatures`→중심점→`map-geo.cellLookup`(칸 크기 버킷 해시, 격자가 완전 정규격자는 아니다)→`setFeatureState({comp,enf,unm,lp,dong})`, 색 식은 `stepExpr(prop,stops,pal,["coalesce",["feature-state",prop],0])`, 테마 갈면 상태가 비므로 `joinedRef` 초기화) ②원→원기둥(`circleColumnsFC`·`weatherColumnsFC`, 반지름은 평면 원과 같고 높이 30~240m, 격자 기둥 켜면 쉼, 흐림은 필터 `["==",["get","dong"],"\u0000"]`로 숨김 — `["==",1,0]`은 레거시 필터 오류) ③시설·후보 점→네모 말뚝(`postsFC`, squareAround)·배치추천→빈 고리(`ringsFC`), 평면/입체 레이어는 `FLAT_ONLY`/`TILT_ONLY` visibility 스위치 ④초점(목록 클릭·드론 목표)=땅 위 고리 레이어 `S.focusRing`+RAF 맥동(DOM 펄스 점 삭제, 라벨만 DOM) ⑤**기둥 전부 불투명**(반투명 기둥과 반투명 건물이 교차하면 건물 벽이 기둥 속에 비쳤다 = "텍스처 깨짐"), NSDI 건물 불투명·구 밖 OSM 0.45 ⑥기둥 켜질 때 `riseColumns`(높이 배율 RAF, 데이터 기반 높이는 transition 미보간). **드론 비행 연속화**: `flyWaypoints`·`flySegmentMs`·`flyCameraAt`(Catmull-Rom 경유지 곡선 + smoothstep 감속·머묾 + 핫스팟 사이 lift 0.7줌, 방위 2.6도/초 등속)을 RAF `jumpTo`로, 진입 구간은 현재 시점→조망 반구간; 경로 점선 `S.flyPath`+번호 `S.flyPts`+안내 띠(`flyInfo`, 지도 컴포넌트 안 오버레이, `fitPadding` 기준 배치). **모달**: `ModalShell`은 `.dump-page` 루트로 `createPortal`(왼쪽 카드 `.dump-fl`의 backdrop-filter가 fixed 기준 상자가 되어 모달이 카드에 갇혔다), xl에선 카드 오른쪽(지도 쪽) 가운데. **법령 팝오버** `law-ref.tsx`: 루트 포털+fixed 좌표(모달 패널 등장 애니메이션 transform이 fixed 기준이 되어 밀렸다), 호버 220ms 지연 닫힘, 클릭 고정(바깥 클릭·ESC 캡처로 해제). **테마 이식 잔여**: `glass-dial.tsx`(유리 강도 `--lg`, localStorage `dump-glass`, `--lg-lo/hi`로 `.dump-fl` 투명도·흐림 연동)·`liquid-glass.tsx`(굴절 런타임 sunlight 복사, `.lg-shell` 요소에 feDisplacementMap, 왼쪽 카드는 `.lg-inner` 서리 층, 글 빽빽한 열·범례·띠는 `.lg-dense`)·`liquid-tabs.tsx`(잉크 캡슐 탭, role=tablist. ★`.lq-tabs`에 CSS position을 주면 Tailwind `md:absolute`를 덮어 탭이 둘째 줄로 떨어진다). 동별 기둥 카드 `.dump-bartip .m` 왼쪽 세로 색띠 제거(유저 규칙: 세로 컬러바 금지). **시연 모드**(`demo` 상태, xl+ "시연" 버튼, ←→·Space·Esc): 결론(자동 회전)→동별 기둥→상습격자→예측 핫스팟 드론→정책 제안(CCTV 재배치 viz) 5장면, 캡션은 map.json·graph.json 수치로만(`scenes` useMemo), 월별 띠 위 캡션 카드. 개발 진단 손잡이 `window.__dumpMap`(NODE_ENV≠production). ★스크린샷은 로컬 `next start -p 3000`이 잡고 있으면 dev를 다른 포트에 못 띄운다(proxy.ts Origin 3000만 허용, Playwright는 Origin 헤더 덮어쓰기 불가) → 3000의 start를 내리고 `next dev -p 3000`. 테스트 267. **18라운드 후속(같은 날)**: 재배치 후보는 잉크색 말뚝·번호(`CAND_COLOR`, 빨강은 과태료 램프·핫스팟과 구분 안 됐다), 시설·후보·배치추천 말뚝이 서면 건물 히트맵은 `mixHex`로 중립색 쪽 55%, 동별 기둥 모드는 건물 중립+라벨 15px·후광 2.6. 호버 툴팁은 전부 카드형 `map-geo.tipCard`(꼬리표·제목·배지·지표 타일(색 칩·큰 숫자·단위·구 최댓값 대비 막대 `gridMaxes`)·각주, CSS `.dump-tip`). 시연 장면마다 카메라 큐 `DumpingMap cameraCue`(bounds·maxZoom·pitch·bearingDelta·duration, easeTo 가감속) → 도착 즈음(1.6초 뒤) 기둥이 솟고 orbit이 이어진다(orbit은 `isEasing()` 중엔 setBearing 안 함. setBearing=jumpTo라 이동을 끊는다). 장면 5는 후보 1·2위 주변으로 내려감. **3D 아이콘·색 통일(2026-09-20)**: [components/dumping/icons3d.ts](components/dumping/icons3d.ts) = maplibre 커스텀 레이어(renderingMode 3d) 위 Three.js(`three` 0.186) 장면. 시설 종류마다 조립 모델(이동식 CCTV 기둥+머리, 고정 CCTV 기둥+돔, 의류수거함 상자, 재활용정거장 받침+통 3, 가로쓰레기통 통), 재배치 후보 = 앰버 핀+머리 위 순위 배지(캔버스 스프라이트, depthTest 없음), 동별 기둥 1~3위 배지(`dongRank`, `dongColumnsFC().ranks`, 기둥 높이 h에 띄움, 900ms 뒤), InstancedMesh + 화면 최소 높이(26px, 시설은 실물 4배 상한·핀은 60배). 투영은 `transform.getMatrixForModel(ANCHOR)` × `defaultProjectionData.mainMatrix`(모델 공간 x동·y위·z남). 지도 `load` 뒤 동적 import로 `addLayer(icons, S.infraPosts)`, 테마 교체 때 커스텀 레이어는 복사 목록에서 빼고 style.load에서 재추가, `idle`마다 `refreshElevation`(지형 고도). 툴팁은 같은 자리의 **투명 fill-extrusion**(opacity 0, `S.infraPosts`·`candPosts`·`recoRings`)이 queryRenderedFeatures로 받는다(커스텀 레이어는 조회 불가). ★MeshPhysicalMaterial은 환경맵 없이 검게 나온다 → Lambert+emissive. 색 문법 통일: 민원 `COMP_COLOR` #3f4f66(잉크 청회)·과태료 `ENF_COLOR` #c0741a(앰버)·상습/핫스팟 벽돌(#a8322a/#b8756a), 채널 스택은 청회 진하기, 라벨 후광 종이색. 팝업(`.maplibregl-popup-content`)·초점 라벨은 유리(블러+반투명 종이). 반영 해제 ✕·후보 목록 ✕가 지도도 기본으로 되돌린다(`clearApplied`, 들어갔다 못 나가던 문제). 개발 진단 `window.__dumpMap`. ★Playwright 시나리오에서 레이어 줄 클릭은 토글이라 앞 시나리오 상태에 따라 꺼질 수 있다(캡처에 아이콘이 없으면 상태부터 확인). **후속 2(같은 날)**: 순위 배지는 스프라이트(2D) → 구·동전(원이 기둥 폭과 안 맞아 화면을 덮음, 유저 거부) → **기둥·핀 꼭대기에 선 입체 숫자**(`makeDigit`: TextGeometry 압출, 서체는 [scripts/dumping-digit-font.mjs](scripts/dumping-digit-font.mjs)가 SUIT Bold(OFL)에서 숫자 0~9 윤곽만 뽑은 `components/dumping/digit-font.json` 2.9KB, opentype.js devDep). 숫자는 카메라 방위를 따라 선다: `rotation.y = -bearing`(모델 행렬이 x를 뒤집어 부호 반대, 방위 0·90·120·200 실측). 동별 숫자 높이 78m(기둥 변 96m 안), 화면 최소 22px, 색은 제 기둥 색. 동별 숫자는 제 기둥(민원 왼쪽·과태료 오른쪽) 위라 안 겹친다(`DongRankBadge.side`는 숫자가 기둥 폭보다 클 때 바깥으로 비킴). **동 기준점 = 동주민센터**(2026-09-20 지시): [lib/dumping/dong-centers.json](lib/dumping/dong-centers.json) 15곳(카카오 키워드 검색 실측, ★.env.local 키 값에 따옴표가 붙어 있어 curl로 직접 부르면 벗겨야 한다), `map-geo.dongCenter(rings, name)`이 이름으로 찾고 없으면 꼭짓점 평균, `dongAnchors()`가 주민센터가 너무 가까운 쌍(중곡1동·2동 136m)을 `DONG_MIN_GAP_M` 420m까지 벌린다(라벨·동별 기둥 공용). 동 라벨은 `text-allow-overlap`(바탕 라벨보다 우선), 동별 기둥 라벨은 `text-variable-anchor`(top·left·right·bottom)로 겹치면 옆자리. **바탕 없음·실사 건물(2026-09-20)**: `BaseMode`에 `"none"`(격자 안 칠함, `BASE_DEF`는 `DataBase` 4종만). 건물 기본색은 `map-geo.realBuildingExpr(theme)` = 층수(`flr`) 보간 5단(1~2 단독 베이지·3~4 다가구 벽돌·5~9 콘크리트·10~19 회백·20+ 유리 청회, `REAL_BUILDING` 라이트/다크) + UFID 끝 두 자리로 ±1층 흔들림(이웃이 같은 색으로 안 붙게). 바탕이 데이터면 히트맵, 없음이면 실사(동 선택·동별 기둥에도 실사 유지). 범례는 층수 띠. ★같은 레포에서 다른 세션이 /snow 작업 중이면 전체 lint·테스트가 그쪽 미완성분으로 깨진다 → `eslint components/dumping lib/dumping`·`tests/dumping-*.test.ts`만 돌리고 `git add`는 파일 지정. **청소차**: `map-geo.routeChains`(도로명별 링크를 끝점 30m 탐욕 연결, 300m 미만 버림, 긴 순) → `icons3d.setTrucks`(체인 2.5km 초과 2대·아니면 1대, 9m/s 왕복, 앰버 적재함+잉크 운전석+흰 경광등, InstancedMesh 매 프레임, 화면 26px 유지·10배 상한). 노선 effect가 `iconsReady` 뒤 공급. **아이콘 한 벌** [components/dumping/icons.tsx](components/dumping/icons.tsx) `Ico`(20 격자·선 1.7·둥근 끝, tilt/orbit/drone/bars/columns/truck/pin/ring/monitor/mic/ear/speaker/search/sun/moon/drop/arrow/close/layers) — map-controls·dashboard·glass-dial·theme·qa-chat의 인라인 SVG 전부 교체. 브라우저 탭 아이콘 `app/dumping/icon.svg`(dump-mark 도형, Next 라우트 세그먼트 아이콘). **19라운드(2026-09-21, 시연 전 프로덕션 최종 점검, [docs/dumping-production-review-2026-09-21.md](docs/dumping-production-review-2026-09-21.md))**: 덱 260922 8장 4단계·시연 모드 5장면·라이브 ask/tts·음성 정책을 프로덕션에서 전수 실측(P0 없음). ★회전·드론 fps 17~27의 병목은 지도가 아니라 **유리 굴절**(`.lg-shell` 10개의 `backdrop-filter: url(#feDisplacementMap)`, 지형·건물·전 레이어를 꺼도 31fps, 굴절만 꺼야 46~56) → `dumping-map`이 `move`마다 `html.lg-moving`을 붙이고 240ms 뒤 떼며, `globals.css`가 그동안 굴절 대신 `.dump-fl` 흐림(`!important`, `.lg-inner` 흐림도 쉼)으로 덮는다(로컬 25→50fps). 자료 종수는 덱과 같은 **22**(구청 7·공개 9·서울시 6)로: 16라운드에 공개 목록에 들어간 "지도 바탕·건물 입체·지형"을 `DatasetGroup note`(종수 제외 표시용 자료, NSDI 건물 출처 포함)로 내림. ★근거 그래프 "다가구·단독 밀집"은 동명 노드 둘(요인·개념 con-unmanaged 연결 12 / 분석 변수 cov-unmanaged 증거 9) → 시연은 검색창에서 요인·개념 선택. ★프로덕션 `DUMPING_PASSWORD`는 로컬 `.env.local`과 다르다(둘 다 4자리). ★헤드리스 스위프트셰이더 캡처는 setData 반영이 수 초 늦어 장면 5에 앞 장면 기둥이 남은 것처럼 찍힌다 → 헤드 Chrome(`channel:"chrome"`)으로 재확인. `dem dimension mismatch` 콘솔 오류(maplibre backfillBorder, DEM pmtiles z8~14)는 화면 영향 없음·보류. 다른 세션이 3000에 `next start`를 띄워 두면 `next build`도 그쪽 `.next`를 덮으니 로컬 검증은 `next dev -p 3001` + curl 로그인 쿠키(Origin 3000 헤더) 주입으로.

8. **광진 제설 상황판** (`/snow`, 공개·인증 없음) — "눈 오기 전에 어디가 비었는지" 보이는 겨울철 제설대책 상황판. 1라운드 2026-09-20(e56bb0b, dumping 복제), **2라운드 같은 날**(데이터 조사부터 재구축, 냉독 46→63, 기록 [docs/snow-production-review-2026-09-20.md](docs/snow-production-review-2026-09-20.md), 데이터 조사 정본 [docs/snow-data-survey.md](docs/snow-data-survey.md), 그래프 설계 [docs/snow-ontology.md](docs/snow-ontology.md)). /dumping의 지도 전면 레이아웃·유리 CSS·ModalShell·ThemeSwitch·LiquidTabs·SectionHead·`lib/dumping/basemap-style.ts`·`onto-view.ts`·`dong-centers.json`을 import만 하고 수정 0. 도메인 코드는 전부 `lib/snow/`·`components/snow/`·`scripts/snow-*.mjs`. **데이터**(`data/snow/raw/`, 평문): 열선 정본 = 서울 열린데이터광장 OA-22584 광진 **55행**(2026-05, ★설치위치 칸 줄바꿈·쉼표 → 따옴표 CSV 파서), 구 15142397 41행은 시점~종점 지번 조인으로 노선명·차로수 보충(39행 일치) · 제설함 15066599 110 · 염화칼슘함 15041574 228(★열 이름 GRS80TM인데 값은 WGS84) · 모래주머니 15041576 45(2022) · **행안부 적설취약구간_도로 15162108 광진 47**(고갯길 37·급경사 8·기타 2, 관리청명 3종 혼용·길이 76·30은 m 혼입) · **행안부 상습결빙구간 15067396 광진 9**(전부 간선·자동차전용도로. ★총길이·방위각이 두 점과 안 맞고 도로명이 OSM과 다름(동부간선도로 좌표가 강변북로 램프) → 두 점만 믿음. ★파일 첫 열에 CP949 깨진 BOM 뒤 진짜 BOM이 한 번 더) · 나이스 초등학교 21(`schoolinfo-mcp/.env.local` NEIS_API_KEY, 좌표는 카카오 키워드) · 기상청 결빙일수 서울 108(`selectFrzDownload.do` 폼 POST) · 도로교통공단 결빙사고 다발지역 15058135 API(활용신청 완료, 2017~2025 광진 0곳, 빌드 시 갱신 캐시) · 도로망 `gwangjin.pmtiles` roads z15 · 지형 `dem.pmtiles`. 못 구한 것(정보공개청구): 동주민센터 담당 구역 구간표(광진구 열린데이터광장 158개 전수 0, 정보소통광장 결재문서 280건에 구간표 미확인 → 행정동 경계 대용 `Zone.proxy`), 취약지점 89개소 목록(보도자료 hwp 첨부), 살포기 52대 위치, 시한 준수·민원. **빌드** [scripts/snow-data.mjs](scripts/snow-data.mjs)(`npm run snow:data`) + [scripts/snow-roads.mjs](scripts/snow-roads.mjs)(★외부 라우팅 0: pmtiles roads → 노드 3만 그래프, 노선명 가중 다익스트라 3후보 중 기대 물리 길이(1차로 연장 ÷ 차로수. 2차로 278m 구간의 도로 경로 163m 실측)에 가장 가까운 것, 두 점 같으면 도로 따라 연장만큼(point), 직선의 3배 넘으면 직선. 열선 55: named 25·network 12·point 13·straight 5. ★2025-12 신설 4구간(구의동 38-20 등 기점=종점 145~190m)은 천호대로 아차산역 램프 위 · DEM 급경사 `steepSegments`: terrarium z14 × 이름 있는 이면도로 20m 표본 100m 창 8~20%(★30%대는 고가·제방 고도 혼입 오탐), trunk 120m 안 제외, 117구간 16.2km "추정" · 공백 판정: 취약구간마다 60m 안 열선·100m 안 자재, 초등학교 150m(이 화면의 가정)). 결과: 취약구간 56 중 열선 없음 17, 공백 5(능동로 120 + 결빙구간 4), 열선 없는 동 4(중곡1·자양3·자양4·군자), 열선 없는 초등학교 16/21. **그래프**: 종류 13(1라운드 11 + `Zone` 담당 구역·`Entity` 취약구간 실체)·관계 23(+assigned·within·exemplifies, covers 레인지 Zone|Entity), 162노드 538엣지, 오류 0. 관측·판단은 `label`(16자 명사형)과 `gist`(합니다체 문장) 분리. 역량 질문 10(`core` 6 기본 노출: 취약구간 공백 17/56·열선 없는 동 4·근거 미연결 1(민관협력. 나머지는 `pol-period` basis)·측정 불가 2·보도자료만 4·기준일 2022~2026). 그래프 화면은 Entity 56을 안 그림(`viewGraph`), 기본 층별 흐름·허브 `kpi-coverage`·이웃만 라벨. **단계** [lib/snow/stage.ts](lib/snow/stage.ts) `stageForSnow(cm, warning)` = 24시간 신적설과 대설주의보(2단계)·경보(3단계) 중 높은 쪽, `inSnowSeason`(11/15~3/15) 밖이면 슬라이더 시나리오 기본(헤드라인 "시나리오:"). 예보 [app/api/snow/forecast/route.ts](app/api/snow/forecast/route.ts) = 기상청 단기예보 nx62 ny126(★SNO는 문자열 "적설없음"·"0.5 미만"·"5.0 이상", 발표 02·05·…·23시+10분) + 특보 현황 stnId 109 t6 텍스트, 실패 시 Open-Meteo best_match(★kma_seamless는 전부 null). **UI**: 탭 5(공백(첫)·대응 단계·자원 현황·근거 그래프·법령·책임. "온톨로지" 단어 화면 금지), 다크 기본(`app/snow/page.tsx` 초기 스크립트가 저장 없으면 dark. ★저장 키는 /snow 전용 `snow-theme`(3라운드. dump-theme를 읽으면 /dumping 라이트 사용자에게 라이트로 떴다), 대시보드 MutationObserver가 저장), 액센트 청빙 #7cc0e8/#2a6f97, **색 정본 `lib/snow/labels.ts`(3라운드)**: 열선 노랑빛 호박 #ffb703(라이트 #c98a00, `HEAT_STYLE` 글로우 #ffd166·흐름 #fff4d6)는 데이터 색(밤 지도 유일 난색), 자재 청회 #8fa3c0·청빙 #7cc0e8·모래 #c9a961 테두리, 취약·결빙 진홍 #e5484d(라이트 #b01e33, `RISK_STYLE` 케이싱·배지 후광), 급경사 추정 보라 #b48ee8/#6d4fb3. `tests/snow-copy.test.ts`가 hue 차 40° 이상 단언. 지도 [components/snow/snow-map.tsx](components/snow/snow-map.tsx): NSDI 건물(★필드 `h`·`flr`, 입체에서만)·마스크를 바탕 라벨 위(구 밖 지명 가라앉힘)·열선 글로우+본선+흐르는 점선(RAF dasharray 회전)+줌아웃 위치 점(자재 원 위, maxzoom 14.2)·취약구간 실선+번호 배지·결빙 점선+"결빙 n"·급경사 점선·학교 흰 원·동별 기둥은 자원 탭에서만(동주민센터 위치, 두 줄 라벨 variable-anchor, 상한 620m)·단계별 상태(`stageView`: 평시·보강 자재 흐림, 1단계 점등, 2단계 제설함 1.6배, 3단계 열선 없는 동 외곽 벽돌)·`focusHeat`(구간·카드 클릭 시 관련 열선만 진하게)·`styleSeq`(★테마 전환 데이터 effect 실사고). 카드 불투명 0.96(`.snow-page .lg-inner`), 시연 5장면(공백›열선›단계 격상 자동 진행+캡션 동기›열선 없는 동›조례 시한), 데이터·방법 모달 5섹션(쓰인·못 구한·가공·그래프 규약·한계), 발견 카드 6(facts 파생), "지금 보는 곳" 칩. **카피 게이트** [tests/snow-copy.test.ts](tests/snow-copy.test.ts): 화면 문자열·그래프 라벨·역량 질문에 비유 동사(겨냥·받친다·잇다·붙다…)·줄표·화살표·슬로건·AI 관용어·OWL 0, 합니다체(반말 정규식 `(?<!니)(?<=[가-힣])다\.`), 12px 미만 0. 테스트 `tests/snow-{schema,queries,stage,copy}` 24. ★스크린샷은 `next dev -p 3000`(★rtk 훅: grep은 `-F`, `\|` 정규식 깨짐)·Playwright character-card, 결빙 배지 minzoom 12, 모바일 탭은 `TABS_SHORT`. **3라운드(2026-09-20 밤~21 새벽, 기록 review 8절)**: ①도로망 타일 봉합(`stitchTileSeams`) 뒤 선형 검증 `scripts/snow-verify.mjs`(물 위·구 밖·우회 위반 0 = `pretest` 게이트, `snow:data` 뒤 자동, ★`docs/snow-data-survey.md` 4절 표를 마커 사이에 다시 쓴다) · `walkAround`는 같은 이름·종류 중 가장 곧은 쪽(램프 열선 U자 고리 실사고) · 좌표 한 점·노선명 못 붙는 취약구간은 60m(`unnamedSameCap`, 능동로 120) · 결빙 points 행은 끝점 공유 묶음 라벨 "결빙 1·2 / 선형 미확인" ②급경사 보라 점선+케이싱, 켜면 칩 "추정 · 급경사 114구간 · 열선 없음 71" ③**3D 핀 [components/snow/icons3d.ts](components/snow/icons3d.ts)**(dumping 복사, 수정 0): 제설함 상자·염화칼슘 원통·모래 포대·학교 깃발·열선 구슬(줌 14.2 아래)·결빙 끝점 고리·입체 숫자(열선 없는 취약 번호 줌 13.6부터·결빙·동별 1~3위). 평면=원·배지, 입체=투명 말뚝 `S.posts` 툴팁+3D(`setDim`·`setBoost`로 단계 문법). ★자재 목표 13px·확대 상한 6배(22px·8배는 z16에서 28m 원통이 동네를 덮었다) ④밀도: `components/snow/ui.tsx` StatBand·NumRow·Table, 상자 게이트 탭당 2 ⑤**보고받는 사람 관점 = 소관 분리**: 공백 7곳 중 6곳이 시 관리 간선 → 첫 화면 "적설취약구간 47곳 중 13곳 열선 없음 · 자재도 없음 1(능동로 120) · 시 관리 결빙 둘 다 없음 6/9"(★분모는 근거 그래프와 같은 47. 구 관리 결빙 3은 열선 있음), `facts.segOwner`·`gapSummary.gu/si`·`buildChecklist`(눈 오기 전 점검 후보 5 = 공백 탭 02·시연 장면 5), 공백 표에 소관 열, 헤더 상태 한 줄(대책기간 안: 단계·적설·특보), 대응 단계 탭 순서 단계›동원›시한, 탭 전환 시 카메라 초기화 ⑦**★LineAtlas 고갈(2라운드 잠복, 냉독이 발견)**: 열선 흐름 RAF가 66ms마다 새 dasharray → maplibre LineAtlas 512행이 1분 뒤 가득 → addDash null → 렌더 예외로 그 위 레이어(라벨·기둥·3D) 소실. 위상 24개 양자화(`FLOW_STEPS`). dasharray를 애니메이션할 땐 패턴 수를 고정할 것 ⑧열선 있는 취약구간은 탁한 색·가늘게(`weakColorExpr`), 열선 없는 구간만 진홍+케이싱(★maplibre는 `case` 안에 zoom interpolate 둘을 거부 → interpolate 안에 case). 냉독 63→66→68(2차, 반영 기록 review 8-8·8-9) ⑥시연: orbit(장면 1·4)·riseColumns·focusRing·격상 진행선·카드 접기·자재 핀 점등. 실측 fps(헤드 Chrome) 1440 회전 100+. 테스트 298. 남은 일: 담당 구간표·89개소 목록 확보 시 교체, 서울시 제설함 Sheet(OA-22648) 실키, 실기기(390) fps.

## Development Commands

```bash
npm install      # Install dependencies (uses pnpm-lock.yaml but npm works)
npm run dev      # Start development server (http://localhost:3000)
npm run build    # Production build
npm run lint     # Run ESLint
```

## Environment Setup

Create `.env.local` with:
```
KAKAO_REST_API_KEY=your_kakao_rest_api_key
```

Get API key from [Kakao Developers](https://developers.kakao.com/) - create app, copy REST API key, add web platform with your domain.

## Architecture

### Tech Stack
- Next.js 15 (App Router) with React 19
- Tailwind CSS 4 + shadcn/ui components
- Leaflet.js for maps (loaded dynamically via CDN)
- xlsx library for Excel export

### Core Data Flow

1. **Client Component** ([components/address-generator.tsx](components/address-generator.tsx))
   - Main UI component handling user input, state management, batch processing
   - Calls API routes for address resolution
   - Supports Excel 2-column paste (address + facility name)

2. **Client-side Resolver** ([lib/utils/address-resolver.ts](lib/utils/address-resolver.ts))
   - `resolveAddressDisplay()` - cached address resolution for single lookups
   - Calls `/api/resolve-address` endpoint

3. **API Routes**
   - [app/api/resolve-address/route.ts](app/api/resolve-address/route.ts) - Single address lookup (full resolution with all formats)
   - [app/api/resolve-address-batch/route.ts](app/api/resolve-address-batch/route.ts) - Batch processing with adaptive rate limiting
   - [app/api/geocode/route.ts](app/api/geocode/route.ts) - Simple geocoding (returns only lat/lon) for Tableau Geocoder

4. **Kakao API Integration** ([lib/utils/kakao-api.ts](lib/utils/kakao-api.ts))
   - `resolveAddress()` - Main resolution function combining multiple Kakao APIs
   - `kakaoSearchAddress()` - Address search API
   - `kakaoKeywordSearch()` - Keyword/place search API
   - `kakaoCoord2Address()` - Coordinates to address
   - `kakaoCoord2Region()` - Coordinates to region (for admin dong)
   - `removeApartmentUnit()` - Extracts unit info (동/호) from address strings

### Address Resolution Strategy

The resolver uses a multi-step approach:
1. If input contains building keywords (학교, 병원, 주민센터, etc.) → try keyword search first
2. Try address search API; if 0 results and the input names a 구, retry without the 구 token (wrong-gu inputs like "서울 성동구 광나루로 614" are common — Kakao returns nothing for them but resolves the gu-less form)
3. If address search fails → fallback to keyword search, but only take a result whose 도로명/법정동/지점명 agrees with the input ([lib/utils/address-hints.ts](lib/utils/address-hints.ts) `pickConsistentKeywordDoc`); otherwise the first hit is returned as `partial` (2026-09-17 서울페이 실측: blind first-hit picked other branches/동 — "건대후문점"→"중곡점", "골드"→"모텔골드")
4. Convert coordinates back to standardized address format

`resolveAddressStrict` (facility import, address search only) trims tokens from the end, then: gu-less retry → same-parity neighbor building numbers ±2·±4 (`partial`, message names the number used) → road-only `ROAD` match as the very last resort (`partial`, "도로명만 일치" — never treat a road-representative point as an exact match; it can land in the wrong 동) → fallback. Fallbacks still carry `meta.adminDong` when the address itself states it (1:1 법정동 화양동·능동·광장동·군자동, defunct 노유동/노유1·2동→자양4동, 모진동→화양동, or a written 행정동 like 자양4동). The facility dashboard writes a `partial` result's message into the facility `memo` so estimates stay visible. Kakao itself already tolerates "(화양동)" 보조표기, trailing 층/호/상호, "번지", "로 N길" spacing and 행정동-written jibun ("자양4동 785-1") — don't add normalizers for those.

### Output Formats

`ResolvedDisplay` type contains:
- `display`: Formatted string like "광진구 아차산로 400(자양동 870, 자양2동)"
- `meta`: Structured data with gu, roadName, buildingNo, unit, legalDong, jibunNo, adminDong, postalCode, coordinates

### Path Aliases

Uses `@/*` mapping to project root (configured in tsconfig.json):
```typescript
import { Button } from "@/components/ui/button"
import { resolveAddress } from "@/lib/utils/kakao-api"
```

## Chrome Extension (`extension/`)

### Tech Stack
- Plasmo framework (Manifest V3), React 19, TypeScript, Tailwind CSS 3
- `@plasmohq/storage` for chrome.storage persistence

### Extension Commands
```bash
cd extension
npm install       # Install extension dependencies
npm run dev       # Dev mode with hot reload
npm run build     # Production build → build/chrome-mv3-prod
npm run package   # Package as .zip for distribution
```

### Structure
- **popup.tsx** - Main popup UI (single/batch conversion, 7 output formats, history, favorites)
- **background.ts** - Service worker (context menu, keyboard shortcut `Ctrl+Shift+C`, auto-detect handler)
- **content.ts** - Content script (clipboard address auto-detection on web pages)
- **options.tsx** - Settings page (API server, default format, map provider, notifications, auto-detect)
- **lib/api.ts** - API client (calls web app's `/api/resolve-address` and `/api/resolve-address-batch`)
- **lib/types.ts** - Types, constants, field labels/examples
- **lib/storage.ts** - History (max 20 + unlimited favorites), settings management
- **lib/format.ts** - Field value extraction for 7 output formats

### Key Features
- 7 output formats: 표준형식1, 표준형식2, 도로명주소, 지번주소, 행정동, 우편번호, 세부주소
- Context menu "표준주소 변환" on text selection
- Clipboard shortcut with desktop notification
- Content script auto-detection with Korean address regex pattern
- Independent popup window mode (stays open after clicking away)
- Configurable API base URL (defaults to https://gjdong.vercel.app)

## Key Implementation Details

- **Rate Limiting**: Batch API uses adaptive batch sizing (5-7 addresses) and delays (80-100ms) based on volume. Automatically reduces batch size and increases delays on high error rates.
- **Batch Chunking**: Client sends requests in chunks of 10 addresses with retry logic (max 2 retries per chunk)
- **Unit Extraction**: Complex regex in `removeApartmentUnit()` handles various Korean apartment unit formats (동/층/호)
- **Building Keywords**: `BUILDING_KEYWORDS` array triggers keyword search for places like 주민센터, 학교, 병원
- **Caching**: Client-side FIFO cache (100 entries max) in address-resolver.ts - evicts oldest entry when full
- **Map**: Leaflet loaded dynamically via CDN, supports batch markers with numbered pins
- **Excel Input**: Supports 2-column paste (address + facility name) detected via tab or 3+ spaces
- **Encoding Support**: Tableau Geocoder supports UTF-8, EUC-KR, CP949 for Korean CSV files
- **Facility Column Inference** ([lib/facility-column-inference.ts](lib/facility-column-inference.ts)): `parseFacilityTable()`/`parseFacilityText()` auto-detect address/name/serial/category columns from pasted or uploaded tables regardless of column order or header presence (header keyword matching first, then per-column data scoring for headerless tables). Detected category/admin-dong columns become dynamic filters in the dashboard. Covered by `tests/facility-column-inference.test.ts`.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
